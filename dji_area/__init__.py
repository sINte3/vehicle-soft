# -*- coding: utf-8 -*-
"""dji_area -- учёт площади DJI по модели доказательств.

Версия модели: ``dji-area-evidence-2026-09-08-final-1``. Нормативные документы
лежат вне репозитория (``C:\\VehicleSoft_Astra_Blind\\OUTPUT\\FINAL``); их
SHA256-манифест скопирован в ``docs/dji_area_evidence/LOCKED_SHA256_FINAL.json``.

ЧЕГО ЗДЕСЬ НЕТ. Flask, SQLAlchemy, ``models``, ``app``. Пакет импортируется
и веб-приложением, и инструментами пересчёта, и тестами -- и ни один из них
не обязан ради него поднимать приложение (``create_app()`` зовёт
``db.create_all()`` на импорте и превращает читателя в писателя). Единственная
внешняя зависимость -- чистая функция ``walk`` из
``drone_collector/route_decode.py`` (stdlib, без ввода-вывода), по тому же
правилу, по которому ``drone_useful_area.py`` берёт ``area_study``.

ЧТО ЭТОТ ПАКЕТ НЕ УТВЕРЖДАЕТ. Ни одно число здесь не является уникальным
физическим покрытием, площадью поля или счётом заказчику. ``raw_area_m2`` --
то, что записал DJI; ``controller_delta_area_m2`` -- проверенная разность
счётчика площади за интервал; ``corrected_recorded_area_m2`` -- техническая
оценка записи с методом и статусом. NULL здесь никогда не ноль.
"""

# Версия модели доказательств. Меняется только вместе со СМЫСЛОМ решений
# резолвера, а не при правке опечатки; хранится рядом с каждым расчётом.
MODEL_VERSION = 'dji-area-evidence-2026-09-08-final-1'

# Версия алгоритма резолвера площади, как она ложится в
# ``dji_area_calculations.area_algorithm_version``. Суффикс ``impl-N`` --
# ревизия реализации внутри той же модели: любое изменение кода, способное
# изменить хоть один статус, обязано поднять суффикс.
#
# impl-2: файл V4 с чужой идентичностью больше не даёт оценку применения
# (наблюдаемые дельты при нём сохраняются как диагностика связанной ревизии);
# негодный V4 даёт уверенность LOW, как требует строка «Bad/incomplete V4»
# решающей таблицы; свидетельство канала считается по полному календарному
# месяцу и больше не зависит от дат в командной строке. Подъём суффикса -- то, чем
# такие правки доходят до уже записанных строк: отпечаток входа несёт версию,
# поэтому пересчёт перепишет их, а не ответит `unchanged`.
AREA_ALGORITHM_VERSION = MODEL_VERSION + '-impl-2'
FIELD_RESOLVER_VERSION = 'dji-field-tiers-2026-09-08-impl-1'
STRUCTURAL_RULE_VERSION = 'structural-retained-screen-frozen-1'
V4_PARSER_VERSION = 'v4-parse-1'
CHANNEL_CAPABILITY_REVISION = 'app-channel-hw-month-1'

# Часовой пояс отчётности: Asia/Tashkent, UTC+5 без перехода на летнее
# время. Record относится к дню НАЧАЛА в этом поясе ровно один раз.
REPORT_TIMEZONE = 'Asia/Tashkent'
REPORT_UTC_OFFSET_HOURS = 5
