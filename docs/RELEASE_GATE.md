# RELEASE_GATE — что влито в `main`, но ещё не провалидировано

Один файл на весь проект. Его читают все треки, потому что друг друга они не
видят, а репозиторий видят.

**Правило.** Строка кладётся **последним коммитом внутрь PR** — тогда она
приезжает в `main` вместе с мержем сама собой (мерж-коммит, созданный кнопкой
GitHub, правок файлов содержать не может). Для задач Fable это обязательный
финальный шаг промпта. Кто закрыл проверки — удаляет свою строку.

**Прод-деплой разрешён только когда пуста таблица «Открытые пункты».** Она и
есть гейт; раздел «В работе, ещё не влито» — объявление по правилу 12, он ничего
не блокирует: там лежит то, чего в `main` ещё нет, и уехать на прод оно не может
по определению.

**В норме файл пуст.** По уставу проверка идёт до мержа, поэтому строка
появляется только как **остаток** — то, что пришлось смержить раньше проверки.
Если строки заводятся и снимаются через час, файл превращается в шум и на него
перестают смотреть.

**Дисциплина против конфликтов слияния.** Одна запись — ровно одна строка файла;
добавлять в конец своей секции; **никогда не переформатировать таблицу целиком и
не выравнивать столбцы пробелами**. Одно косметическое выравнивание — и следующий
мерж конфликтует по всему файлу.

Заведён 2026-07-24 после случая, когда инкремент 7 трека Запчастей уехал на
production попутно с релизом трека АЗС `c548c71`, минуя ~25 незакрытых проверок.

---

## Открытые пункты

| Трек | Что влито | Коммиты / PR | Что не проверено | Кто снимает |
|---|---|---|---|---|
| Дроны | `UI-FUEL-BALANCES-REGRESSION-001` — восстановлены осиротевшие в UI-P6-005c классы АЗС; +46 строк в `design-system.css` (общий файл, чистое добавление, 0 удалений) | PR #64, `a785eea` | Вид `/fuel/`, `/fuel/report`, `/fuel/receipts`, `/fuel/manual-expenses` на живых данных глазами — стенд проверял на синтетике (1 склад против 23) | трек Дронов, QA-раунд задания А |
| Дроны | `DRONE-CLOSE-SUBDIV-001` — второй проход имени файла по различающим словам в импортёре, бэкфилл подразделений (dry-run по умолчанию, откат по списку id), подписи обеих корзин закрытия | PR #65, `f1fc84e` | Сухой прогон бэкфилла на production против §5 промпта (105 сирот → 88 решено / 17 остаются, нерешённая только `Сервис Дрон Маълумот (2).xlsx`), `--apply` после совпадения; подписи корзин на живых данных | трек Дронов, QA-раунд задания А |
| Дроны | `MEGA-3` — экран `/drones/reports/health`, пять срезов аномалий со ссылками в первоисточник; read-only, миграций нет | PR #66 | Экран на живых данных не открывался (стенд синтетический); производительность пяти агрегатных запросов на ~31 тыс. вылетов не мерялась | трек Дронов, QA-раунд задания А |
| Дроны | Кампания книг (`BOOKS-VERIFY`, `CASH-DEFAULT`, `CASH-SPLIT`, `SVODKA-READER`, `MARCH-FIX`) — разграничение «долга», подотчёт операторов, читатель «СВОДКА …», сверка базы с книгами | PR #68 | Производственный прогон прошёл 2026-08-11 (бэкфиллы 375+11 и 3 клетки, сверка разгадана, работ 904 → 1088). Осталось: экраны каналов и подотчёта на живых данных; вписать правки в сами книги (11 «получено», март G8→E8, 21 цена) — иначе переимпорт вернёт старое | трек Дронов, QA-раунд кампании |
| Дроны | `DRONE-FLIGHTS-VS-BOOKS-DAILY-001` — посуточная сверка телеметрии с ведомостями (`tools/drone_flights_vs_books_daily.py`, только чтение); июньские книги в манифесте (29 → 31) | PR #69 | Прогон на production за 2026-03..2026-06 и чтение отчёта: итоги месяцев обязаны совпасть с экраном «Ведомости против вылетов»; на синтетике проверено, на живых данных нет | трек Дронов, разговор о марте–июне |
| Дроны | `DRONE-SEPT2025-RECON-001` — разбор сентября-2025 на файлах владельца, инструмент и карта (только чтение) + миграция `DRONES_ASSIGN_SEPT2025_FIX_001`: три машины разделены между двумя операторами каждая, машина 9 остаётся без оператора | PR #84 | Миграция на production НЕ применялась: сухой прогон, сверка предусловия с живыми 39 строками назначений, `--apply`, затем сверка экрана «Вылеты дронов» с числами сверки (Хамроев 415.55, Хасанбой 418.44, Имомов 407.70, Қудратов 199.19, Жумаев 388.40, Жураев 193.94). Отчёт по работам этой миграцией НЕ меняется — он ждёт правок в книгах и переимпорта | трек Дронов, разбор сентября |
| Дроны | `DRONE-LANDS-001` + `DRONE-LANDS-002` — снимок справочника (5 489 контуров) и мост «заказчик книги ↔ контур» по именам; оба прогнаны на production 2026-08-18/19 | PR #85, #86, #87 | Снимок и мост отработали: 5 489 из 5 489, повтор unchanged=5220; мост дал 262 совпадения из 448 строк, площадь сошлась на 156. НЕ проверено владельцем: 132 строки-кандидата, 173 контура без строки книги (639.89 га) и 54 строки книг без контура (568.47 га) — глазами не смотрены. Сшивка ПО КООРДИНАТАМ признана неработающей, её числа использовать нельзя | трек Дронов, разбор сентября |
| Дроны | `DRONE-IMPORT-DUP-001` — отчёт о книгах, загруженных дважды (только чтение) | PR #96, #98, #99 | Сентябрь закрыт: 15 строк / 118.40 га убраны, книга 5610.03 га. НЕ разобраны кандидаты апреля, мая и июня 2026 — суммарно свыше 1 200 га, и по листу `свод ичи (Шахзод)` за апрель и май отчёт даёт `UNKNOWN`: у стороны из книги Сервиса не привязан оператор | трек Дронов, разбор октября и далее |
| Дроны | `DRONE-APPLY-SQL-001` — запускалка того, что владелец раскомментировал | PR #99 | Прогнана на production 2026-08-20, отработала и отказов не дала, но удалила 0 строк: к моменту запуска работа была уже сделана. Путь «действительно удаляет» на живой базе не пройден ни разу — пройдёт на апреле или мае | трек Дронов, разбор октября и далее |
| Дроны | `DRONE-COVERAGE-001-B` — сбор геометрических маршрутов и полных контуров полей в файловую очередь (`--routes`, `--lands --with-geometry`); отправки в Vehicle Soft нет, миграций нет, базы прогон не касается | PR #107 | **Ни одного живого прогона:** кабинет DJI из среды разработки недостижим. Не проверено: подписывает ли сайт `fetch` из своего контекста (иначе код выхода 10), глубина хранения маршрутов у DJI, лимиты частоты, доля контуров с несходящимся `contentMd5`, встречается ли `MultiPolygon`. Порядок первого прогона — `docs/tracks/drones.md` §6 пункт 9а | трек Дронов, первый живой сбор |

**Прод-деплой закрыт, пока таблица не пуста.**

---

## В работе, ещё не влито

| PR | Трек | Что трогает | Статус |
|---|---|---|---|
| — | — | — | — |

---

## Миграции, применённые с последнего пустого состояния

| Миграция | Трек | Коммит | Дата | Площадки |
|---|---|---|---|---|
| — | — | — | — | — |

Таблица очищена 2026-08-09 после релиза **v1.17**. Единственная накопленная
миграция — `DRONES_WORKS_UPLOAD_001` (`120f80a`, PR #52) — применена и на
production, и на staging; реестр `schema_migrations` на обеих площадках —
**37 записей**. На production прогнана трижды, второй и третий прогоны —
«Already applied. Nothing to do.». Якорь совпал до цифры до и после: 904
работы, 15 878.64 га, 713 заказчиков, 336 единиц техники, 31 059 вылетов;
таблица `drone_work_imports` создана и пуста. Бэкап
`transport_20260809_123401.db` (194 252 800 байт, побайтно равен исходному,
integrity ok) снят непосредственно перед деплоем.
**Отступление, записанное как отступление:** шаги проверки дрейфа миграций
пропущены — `check_migration_drift.py` требует аргумент `--db`, а runbook
звал его без аргумента. Ошибка в runbook, не в приборе; прибор прогнан
отдельно после релиза.
**Наблюдение о шуме приборов:** `check_db_lock.py` при остановленных службах
напечатал `CLEAN`, а не записанный за ним `STALE ... DO NOT MIGRATE`, потому
что бэкап через SQLite online backup API закрывает базу штатно и убирает WAL.
Строка `TOOL-DBLOCK-PERSIST-WAL-001` остаётся: шум проявляется, когда службы
гасят без бэкапа.

Таблица очищена 2026-08-06. Обе накопленные миграции — `DRONES_WORKS_001`
(`159b78e`, PR #42) и `DRONES_WORKS_002` (`457d291`, PR #43) — применены и на
production, и на staging релизом **v1.13** (тег `v1.13-production-2026-08-05`);
реестр `schema_migrations` на обеих площадках — **36 записей**. Строки
простояли со значением «production — нет» сутки после того, как это
перестало быть правдой: релиз ведёт тот чат, которому он поручен, и он же
чистит эту таблицу — пропущенный шаг, а не новое правило.

Таблица очищена 2026-08-03 после релиза **v1.12**. Обе накопленные миграции —
`DRONES_FLEET_001` и `DRONES_FLEET_SEED_001` (`17ef6eb`, PR #41) — применены и
на production, и на staging; реестр `schema_migrations` на обеих площадках —
**34 записи**. Каждая прогнана дважды, вторым прогоном «Already applied.
Nothing to do.»; переход реестра 32 → 34 и вывод сида
(`operators=17 batteries=46 statuses=15 hardware_id=13 generator_id=14
subdivision=15 assignments=0`) живут в верхнем блоке-снимке
`docs/AGENT_STATE.md`.

Предыдущая чистка — 2026-08-02, после релиза **v1.11**. Единственная накопленная
миграция — `DRONES_REATTACH_001` (`ac177c2`) — применена и на production, и на
staging; реестр `schema_migrations` на обеих площадках — **32 записи**. До этой
чистки строка утверждала «production — нет», что перестало быть верным в 15:09
2026-08-02. История релиза с точными хешами, порядком прогона и переходом
реестра 31 → 32 живёт в верхнем блоке-снимке `docs/AGENT_STATE.md`.

Предыдущая чистка — 2026-07-30, когда гейт дошёл до пустого состояния после
релиза **v1.10**. Все накопленные миграции — `FUEL_COUNTER_MISMATCH`
(`747d6ad`), `CORE_FOUNDATION_001` (`91df3c5`) и `DRONES_FOUNDATION_001`
(`e1d9aa2`) — применены и на production, и на staging; реестр
`schema_migrations` на обеих площадках — **31 запись**. До этой чистки таблица
утверждала «production — нет» по двум последним, что перестало быть верным в
момент релиза. История релизов с точными хешами, порядком прогона и переходом
реестра 29 -> 30 -> 31 живёт в верхнем блоке-снимке `docs/AGENT_STATE.md`,
который ведёт релиз. Числа площадок в этом файле по уставу не поддерживаются.

---

## Журнал закрытых пунктов

Короткая история, чтобы не спорить потом о том, что здесь висело.

- **2026-08-15 — снято, трек Дронов. Аудит операторов.**
  `DRONE-OPERATOR-AUDIT-001` (PR #81). Прогон на production выполнен
  владельцем, отчёт разобран openpyxl. Все три условия строки закрыты:
  инвариант «сходится» в 13 месяцах из 13, спорных дней 0; книги по датам
  работ совпали с #69 до сотой (935.87 / 6 232.95 / 2 021.42 / 1 665.48);
  телеметрия месяцев совпала с починенным календарём, который сверялся со
  сводкой `/drones/summary` при приёмке миграций 2026-08-13/14 (сентябрь
  5 572.18 подтверждён владельцем на экране). Содержательный итог прогона —
  в брифинге владельцу и в трековом файле: 3 246.72 га книг на
  нераспознанных написаниях, разгадка Имомова (141.2 га датированы до
  первого вылета его машины), пять вопросов к людям.
- **2026-08-14 — снято, трек Дронов. Отчёт незаписанных гектаров.**
  `DRONE-FLIGHTS-MISSING-001` (PR #70). Прогон на production за 2026-03..06
  выполнен, оба условия строки закрыты. Зеркальность с посуточной сверкой #69
  точная по всем четырём месяцам и итогу: −281.75/+281.75, −43.09/+43.09,
  −1070.96/+1070.96, −2045.52/+2045.52, итого −3441.32/+3441.32. Доля книг,
  привязанных к машинам, измерена — 1 064.20 га из 12 089.92, то есть 8.8 %;
  в марте и июне ноль. Это не дефект отчёта, а мера открытого пункта
  `DRONE-FLEET-ASSIGN-001`, и она перенесена в трековый файл.
- **2026-08-14 — снято, трек Дронов. Улики переназначения.**
  `DRONE-REATTACH-001` / `DRONE-CAL-CELL-CLASS-001` (PR #67). Прогон на
  production выполнен, инструмент отработал чисто. **Снято с оговоркой:**
  строка требовала увидеть на живых данных сорок написаний по классам, семьи
  и своды гомоглифов, а после миграций 2026-08-13/14 их осталось три —
  `№001`, `№002Sayful`, `№002`, все август-2025, вердикты `homoglyph=0
  ambiguous=0 unknown=3`. Классификатору больше не на чем работать: данные,
  ради которых он писался, получили машины. Проверить это условие в прежнем
  виде уже нельзя и не станет можно, поэтому строка закрывается тем, что
  есть, а не висит вечно.
- **2026-08-09 — снято, трек Дронов. Экран закрытия.**
  `DRONE-CLOSE-001` (PR #54, мерж `0821389`). Проверки прогнаны на staging
  `0821389`. Миграций нет, схема и данные не менялись, поэтому откат кода
  является полным откатом.
  **Инвариант доказан на живых данных, а не только на фикстуре.** Строка
  «Итого» экрана закрытия совпала со сверкой помесячно: май 2656.82 / 2439.83,
  апрель 6336.15 / 6379.22, март 1056.87 / 1338.62; карточки шапки на обоих
  экранах — 15 893.64 га ведомостей и 10 216.61 га вылетов, расхождение 0.00.
  Сумма по подразделениям сходится с месяцем, включая обе корзины —
  «Подразделение не определено» и «Машина не определена».
  **Цвет проверен приборно, а не глазами:** 47.0 % — `vs-badge-danger`, фон
  `rgb(253,238,238)`, текст `rgb(192,57,43)`; 72.1 % — `vs-badge-warning`,
  `rgb(253,243,227)` и `rgb(180,83,9)`. Совпадает с токенами `--vs-danger` и
  `--vs-warning`. Эквивалентность вынесенных порогов старому коду проверена
  прогоном обеих реализаций на 1 000 021 значении покрытия, включая все восемь
  границ: ноль расхождений. Удалений в `drones.py` ровно семь — те самые семь
  строк блока порогов.
  **Все четыре состояния клетки найдены на настоящих данных**, строка `0.0 %`
  на странице не встречается ни разу. Список «летали, книги нет» отсортирован
  по убыванию гектаров, ссылка открывает реестр с применённым фильтром
  (`/drones/works?period=2026-04`, поле «Период» = `2026-04`).
  **Узкий экран 390×844:** страница `clientWidth == scrollWidth == 380`,
  вбок движется только `.vs-table-scroll` (346 → 1731), при её прокрутке
  `window.scrollX` остаётся 0, шапка не смещается. На 1024×768 то же.
  **Узбекский просмотрен владельцем:** «Ким ведомост топширмаган»,
  «ТОПШИРИЛМАГАН», «парвоз гектарлари йўқ», «Бўлим аниқланмаган», «Машина
  аниқланмаган», «Учган, китоб йўқ». Латиницей — только `Excel`, имя
  собственное; оно вынесено в явный список исключений теста, рядом с
  контролем, доказывающим, что скан срабатывает на подложенной латинице.
  Ошибок консоли и неуспешных запросов — ноль на всех экранах.
  **`FAIL` браузерного агента оказался ошибкой задания, а не дефектом:** я
  потребовал в приёмке окно «последние 6», которого не было ни в промпте на
  код, ни в коде; окна 12/24/все, `?window=6` штатно откатывается на 12.
  Третий инкремент подряд, где часть находок приёмки — мои же ошибки в числах.
  **Первый прогон профиля `qa-staging` для Codex.** Прежний профиль
  `vehicle-soft-audit` запрещал любую запись и не давал даже переключить язык;
  его выходной каталог жёстко ограничен каталогом аудита, из-за чего агент
  дважды получил `Failed to apply patch` при записи отчёта в `C:\qa` и написал
  файл в обход. Заведён отдельный скилл `vehicle-soft-staging-qa-boundary` для
  приёмки инкрементов, у скилла аудита сужен триггер.
  **Находка, записанная как ограничение, а не как дефект.** Экран показывает
  «Гарден Бухоро Агрокластер, 2026-04 — НЕ СДАНА, 994 вылета / 1032.15 га»,
  хотя книга сдана: `Гарден Дрон маълумот Апрель.xlsx`, 16 работ / 488.49 га.
  Эти работы лежат в корзине «Подразделение не определено», потому что у них
  нет ни оператора, ни подразделения из имени файла. На production таких
  **105 работ / 2 445.32 га**. Экран не врёт — он показывает данные как есть,
  но читается как обвинение отчитавшегося подразделения. Диагностика по книгам
  показала, что 88 из 105 закрываются выводом из данных: у 26 книг из 28
  опознанные операторы указывают на единственное подразделение, а две книги
  Гардена закрываются именем файла, потому что «Гарден» входит ровно в одно
  название парка. Остальные 17 — `Сервис Дрон Маълумот (2).xlsx`, операторы
  которой из двух подразделений сразу. Следующий инкремент трека:
  `DRONE-CLOSE-SUBDIV-001`.
  **Не проверено и названо прямо:** на production экран не открывался — на
  staging полётная сторона живёт реплеем и обрывается в мае, поэтому летние
  месяцы там пусты; производительность двух `GROUP BY` на 30 702 вылетах не
  измерялась; доступ к экрану под неадминистратором не проверялся — отдельной
  учётки без прав администратора по-прежнему нет.

- **2026-08-09 — снято, трек Дронов. Два инкремента, гейт пуст.**
  `DRONE-WORKS-UPLOAD-001` (PR #52, мерж `120f80a`) и `DRONE-WORKS-UPLOAD-002`
  (PR #53, мерж `67b0007`). Проверки прогнаны на staging `67b0007`.
  Миграция `DRONES_WORKS_UPLOAD_001` применена на staging: якорь
  **906 работ / 15 893.64 га / 713 заказчиков** совпал до цифры до и после,
  отрицательное постусловие миграции подтвердило неизменность схемы реестра.
  **Дедупликация доказана дифференциально:** партия 1 — «0 добавлено, 52 уже
  были»; партия 2, та же книга под другим именем, — «52 добавлено, 0 уже были»,
  и после удаления партии реестр вернулся ровно к 906 / 15 893.64; партия 4 из
  двух книг — 73 строки / 1 387.64 га, «0 добавлено, 73 уже были».
  **Защита от подмены сработала**, назвав оба размера и оба хеша
  (25 009 / `b435bc56a7cd` против 24 498 / `acb705cfd3cd`).
  **Детектор месяца поймал книгу, подшитую не в тот месяц:** 51 строка /
  1 048.80 га помечены вне объявленного периода, а единственная строка без даты
  унаследовала период формы — после записи апрель вырос на 51 работу и 1 048.80
  га, август на одну работу и 17.00 га.
  **Кириллица прошла весь путь:** `Ғ` (U+0492) даёт `d292` и в имени файла на
  диске, и в `drone_works.source_file`.
  `DRONE-WORKS-UPLOAD-002` проверен настоящей книгой в 75 МиБ, которую прежний
  лимит отвергал: принята, 3 строки / 179.20 га, «0 добавлено, 3 уже были»,
  разбор 0.0 с — вес файла дают встроенные картинки, которых openpyxl не
  читает, поэтому фоновая обработка не нужна. Форма предлагает предыдущий
  месяц, подсказка — 100 МБ и 240 МБ.
  **Приёмку выполнил владелец руками, а не браузерный агент:** у Codex в
  профиле зашита read-only политика для Vehicle Soft, и он остановился на
  первом же изменяющем действии, доложив BLOCKED. Заведён отдельный профиль
  `qa-staging` и локальные правила в `C:\qa\AGENTS.md`.
  **Не проверено и названо прямо:** партия в 240 МиБ целиком не загружалась и
  413 от Werkzeug выше границы не наблюдался; измерен только разбор — полное
  время запроса на большой книге, включая передачу, запись и sha256, не
  измерялось; загрузка под неадминистратором не проверялась, отдельной учётки
  без прав администратора нет.

- **2026-08-08 — снято, трек Дронов. Три инкремента разом, гейт пуст.**
  `DRONE-ANALYTICS-001` (PR #50, мерж `847c374`), `DRONE-SPRAY-MEDIAN-SCOPE-001`
  (мерж `463230f`), `DRONE-UI-FIX-003` (PR #51, мерж `f1ca650`). Все проверки
  прогнаны на staging `f1ca650`. Миграций нет ни в одном из трёх, схема и данные
  не менялись, поэтому откат кода является полным откатом; реестр
  `schema_migrations` — 36 записей до и после.
  **Данные не двигались через три рестарта службы:** `drone_analytics_anchor.py`
  снимался до и после каждого выката, и `Compare-Object` каждый раз давал ровно
  одно расхождение — строку `generated`.
  **Полный прогон на Windows: 481 тест, ноль отказов** (утром было 463). Это же
  прогон и доказал `DRONE-UI-FIX-003/3`: до него
  `test_b3_money_cell_is_declared_exactly_once_in_the_repo` падал на площадке
  каждый раз — `os.path.relpath` отдаёт обратные слэши, а сравнение шло с
  прямыми, — и проходил в POSIX-контейнере. Контроль, который не может пройти
  там, где выполняется, приучает читать красный прогон как норму.
  **Дефект коридора, найденный приёмкой и исправленный в том же круге:**
  `DRONE-ANALYTICS-001` считал медиану по отфильтрованному набору, поэтому
  фильтр по машине делал машину её собственным эталоном. На staging машина №8
  давала медиану 0.43 и объявляла нормой апрель с 0.43 л/га, а красила март с
  27.65 — значение внутри паркового коридора. Цвета были перевёрнуты
  относительно правды. После `DRONE-SPRAY-MEDIAN-SCOPE-001` на той же машине
  медиана осталась парковой 28.98 при коридоре 20.29–37.68: май 0.00 и апрель
  0.43 красные, март 27.65 без цвета. Ошибка была в спецификации, не в коде:
  формулировка «медиана по всем машино-месяцам в отфильтрованном набоpе»
  писалась про фильтр периода и неверна для фильтра машины.
  **Второй дефект того же круга:** календарь красил серым, то есть
  «исправна и не летала, простой не объяснён», каждый день, о котором ничего не
  известно. Журнал статусов — разовый снимок из пятнадцати строк, все от
  2026-08-02, а вылеты кончаются 30.07 на production и в мае на staging:
  пересечения нет ни одного дня. Введено четвёртое состояние `is-unknown` и
  баннер с датой начала журнала, читаемой из данных.
  **Третий:** блок с медианой рендерился нечитаемой лесенкой, потому что
  `.vs-alert` — это `display: flex`, и каждый `<b>` внутри становится отдельным
  флекс-элементом; на отфильтрованном экране их девятнадцать. Содержимое
  обёрнуто в один элемент на трёх экранах модуля; сам `.vs-alert` не тронут — он
  общий на всё приложение.
  **Приёмку экранов выполнил браузерный QA-агент** (Playwright, read-only),
  отчёт от 2026-08-07 по девяти разделам. Пути записи проверил владелец руками:
  назначение из предзаполненной формы сохранилось с исправленной датой 14.04, а
  не с подставленной 01.04; пустая «Дата с» отклонена и на клиенте, и на сервере;
  сумма посчиталась из площади и цены; набранная руками сумма `123` уцелела при
  смене цены с 200 000 на 85 633. Узбекский просмотрен на четырёх новых экранах —
  только кириллица.
  **Три находки приёмки оказались ошибками задания, а не дефектами**, и записаны
  так: покрытие за 2026-03 равно 79.0 %, а не 78.9 % (я округлил отбрасыванием);
  четырёхзначные проценты не группируются по собственному правилу проекта; строка
  «долга нет» показывает настоящий ноль, а не прочерк. Смоук 2026-08-08
  остановился на воротах, где я подставил числа, посчитанные на 2026-08-07, к
  экрану, который считает возраст от сегодня — за сутки одна работа перешла из
  31–60 в 61–90 и две из 61–90 в 91–180, деньги сошлись до копейки; и доложил 404
  на `/drones/reattach`, которого не существует — маршрут объявлен как
  `/drones/units/reattach`. Обе остановки — правильное поведение агента.
  **Не проверено и названо прямо:** содержимое Excel-выгрузок разбиралось
  `openpyxl`, но в самом Excel не открывалось; ветка «литры не записаны»
  недостижима на реальных данных (0 из 28 832 на production, 0 из 10 409 на
  staging) и исполняется только фикстурой; различимость `is-unknown` и `is-idle`
  на живых данных сравнить нельзя, потому что все отображаемые месяцы лежат
  раньше первой записи журнала и пустые клетки в них все `is-unknown`;
  `DRONE_SPRAY_MIN_AREA_HA = 10.0` и полосы покрытия 90–110 / 50–200 % — решения
  владельца, на живых данных не калиброванные; регулярный сбор вылетов
  (`DRONE-SCHEDULE-001`) не заведён, последний вылет на production — 30.07.2026.
  **Оставлено как есть, чужой файл:** `flight_calendar.html` ставит
  `<td class="vs-cal-cell">`, а в CSS определены только составные
  `.vs-cal-cell.is-*` — класс недостижим, дефект пришёл с `dc79aa3`.

- **2026-08-05 — снято, трек Дронов. Пять задач разом, гейт пуст.**
  `DRONE-WORKS-001` (`159b78e`, PR #42), `DRONE-WORKS-IMPORT-FIX-001`
  (`457d291`, PR #43), `DRONE-REPORTS-HUB-001` (`7860703`, PR #44),
  `DRONE-DEBTS-DOUBLE-HEADER-001` (`b9fd73d`, PR #45), `DRONE-UI-FIX-001`
  (`4934fba`, PR #46). Все проверки прогнаны на staging `4934fba`.
  Миграции: `DRONES_WORKS_001` реестр 34 → 35, `DRONES_WORKS_002` реестр
  35 → 36, каждая дважды, вторым прогоном «Already applied. Nothing to do.»;
  дрейф `file-but-not-registered` 0 → 1 → 0 на обеих. На production ни одна
  не применена — это шаг релиза, а не пробел проверки.
  **Импорт 28 книг диспетчеров выполнен на staging:** 904 работы,
  15 878.64 га, 713 заказчиков, 9 дублей пропущено с обоими провенансами,
  0 отвергнутых строк. **Идемпотентность доказана дифференциально:** второй
  прогон `--apply` дал `0 works inserted, 904 already present and skipped`.
  Якорь апреля — 6 336.15 га по ведомостям против 6 379.24 га по нашим
  вылетам DJI, расхождение **0.86 %**; сверка независима, потому что стороны
  измерены разными путями.
  **Первый сухой прогон 2026-08-04 задачу не подтвердил, а опроверг** —
  496 строк вместо ~895, ноль разобранных цен, «получено» в одиннадцать раз
  больше «суммы». В базу не записалось ничего: `--dry-run` — режим по
  умолчанию. Разбор дефекта и вывод про фрагментное сопоставление колонок —
  в `docs/tracks/drones.md`.
  **Приёмку экранов выполнил браузерный QA-агент** (Playwright MCP,
  read-only), отчёты Part A и Part B от 2026-08-05. Права закрыты обеими
  половинами: 403 на восьми маршрутах дронов под учёткой без права `drones`
  и отсутствие пункта меню. Сводка вылетов не сдвинулась —
  10 408 / 10 216.61 за 2025-08-01..2026-08-31, как до коммерческого контура.
  **Дефект, найденный приёмкой и исправленный внутри того же круга:**
  `DRONE-DEBTS-DOUBLE-HEADER-001` — `/drones/works/debts` несла две шапки,
  две формы фильтров и два ряда карточек, причём верхняя форма отправляла на
  **другую** страницу. Внесён `DRONE-REPORTS-HUB-001`, им же и снят.
  Посотовая сверка долгов при выносе на отдельную страницу доказала, что
  таблицы переехали без искажений (60 ячеек, 0 расхождений), но ничего не
  проверяла в том, **что переехало вместе с ними** — отсюда и дубль.
  **Отступление, записанное как отступление:** `DRONE-UI-FIX-001` смержен в
  `main` раньше приёмки — строка гейта завелась как остаток, ровно тот
  случай, под который гейт и заведён.
  **Не проверено и названо прямо:** содержимое Excel-выгрузок агентом не
  открывалось (в задании запрещено, у браузерного агента нет парсера);
  арифметика подсказки по назначениям не пересчитывалась; побуквенные
  кириллично-латинские двойники в узбекском не различались — проверены
  только целые латинские слова.

- **2026-08-03 — релиз v1.12, гейт пуст.** `17ef6eb`, тег
  `v1.12-production-2026-08-03`. Диапазон `292c53e..17ef6eb`: 16 коммитов, два
  мержа (`0bbfcbf` PR #40 `TOOLS-TRUST-001`, `17ef6eb` PR #41
  `DRONE-FLEET-001`), дифф 23 файла / 4722 вставки / 501 удаление. Чужих треков
  в релизе нет: `app.py`, `fuel_routes.py`, `spare_parts.py` и
  `excel_export.py` в диапазоне отсутствуют; оба объявленных по правилу 12
  общих файла — чистое добавление, `models.py` +226/−0 и
  `templates/index.html` +31/−0. Бэкап `transport_20260803_163249.db`
  (189 906 944 байта, побайтно равен исходному, integrity ok).
  `DRONES_FLEET_001` и `DRONES_FLEET_SEED_001`, каждая дважды, реестр 32 → 34;
  сид: operators=17 batteries=46 statuses=15 hardware_id=13 generator_id=14
  subdivision=15 assignments=0 — ноль назначений намеренный.
  **Два прибора подтверждены на живом материале, а не на синтетике:**
  `TOOL-DRIFT-LEGACY-001` — `registered-but-no-file` 15 → 6,
  `unclassifiable` 21 → 12, `resolved-by-backfill` 9 (оба счётчика сдвинулись
  ровно на девять), `SyntaxWarning` исчез, `file-but-not-registered` 0 → 2 → 0;
  `TOOL-DBLOCK-WAL-001` — на staging пара HELD при работающих службах и CLEAN
  при остановленных, то есть тот самый различающий контроль, которого не было
  в v1.11.
  **Отступление, записанное как отступление:** тег создан до завершения смоука
  — второй релиз подряд с тем же отступлением.
  **Не проверено и названо прямо:** переход из сводки в список вылетов с
  применённым фильтром — тоже второй релиз подряд.

- **2026-08-02 — релиз v1.11, гейт пуст.** `292c53e`, тег
  `v1.11-production-2026-08-02`. Диапазон `459f13e..292c53e`: 45 коммитов,
  три мержа (`2e2cccd` PR #37, `00f859a` PR #38, `ac177c2` PR #39), дифф
  47 файлов / 7975 вставок / 146 удалений. Чужих треков в релизе нет:
  `app.py`, `fuel_routes.py`, `spare_parts.py`, `excel_export.py` и
  `sec003a_ext.py` в диапазоне отсутствуют, `models.py` +47/−0.
  Порядок соблюдён: три службы остановлены и подтверждены `Stopped` → бэкап
  `transport_20260802_150815.db` (189 874 176 байт, побайтно равен исходному,
  integrity ok) → проверка блокировки → `git merge --ff-only` → дрейф →
  миграция дважды → дрейф → старт → смоук → тег. `DRONES_REATTACH_001`:
  `Done. Table drone_reattach_runs with 8 columns is in place.`, повторно
  `Already applied. Nothing to do.`, реестр 31 → 32. Дрейф трижды одним
  инструментом: `file-but-not-registered` 0 → 1 → 0, легаси 15 и 21 без
  изменений. Смоук: техники 336, заказчиков 9, балансовый отчёт и рабочий
  стол запчастей открываются, `/drones/units` — 15 машин с кириллическим
  `4 Ғиждувон`, `/drones/summary` за 2025-08-01..2026-08-31 — 28 832 вылета /
  28 835.34 га / 899 с нулевой площадью.
  **Два отступления, записанные как отступления:** `tools/check_db_lock.py`
  прошёл свой отрицательный контроль — один и тот же зелёный вердикт при трёх
  работающих службах и при остановленных (заведено `TOOL-DBLOCK-WAL-001`;
  то, что службы стояли, установлено `Get-Service` и `state_snapshot.py`, а не
  этим прибором); тег создан до смоука, вопреки порядку runbook.
  **Не проверено и названо прямо:** переход из сводки в список вылетов с
  применённым фильтром — путь, который чинил `a6fe1e1`. Экран
  `/drones/units/reattach` не прогонялся: на смоуке открывали `/drones/reattach`
  и получили 404, это неверный URL в runbook, а не дефект.

- **2026-08-01 — снято, трек Дронов.** `DRONE-007` (PR #39, мерж `ac177c2`;
  коммиты `b80a572`, `eb58ba0`, `5bb8cce`, `b05230e`, `594b889`). Строка
  завелась как остаток: PR смержен раньше приёмки. Проверки прогнаны на
  staging `594b889` **после** мержа, и до их закрытия эта строка держала
  прод-деплой закрытым. Миграция `DRONES_REATTACH_001`: реестр 31 → 32,
  повторный прогон «Already applied»; отдельно проверено, что без файла базы
  скрипт выходит с кодом 2 и файла не создаёт, а при непройденном
  предусловии откатывается и записи в реестре не оставляет.
  Приём: фикстура 21 вылет — `seen 21 / new 21 / duplicates 0 / unresolved 17`,
  повтор того же батча — `new 0 / duplicates 21`, то есть дедуп доказан
  **дифференциально**. Экран алиасов: добавление, отказ по дубликату с
  указанием машины вместо 500, предупреждение о неоднозначности нормализации,
  отключение и включение обратно. Фильтр `is_active` проверен через ту же
  функцию `_drone_nickname_maps()`, которой пользуется приём: отключение
  `GardenN6` перевело группу из распознаваемых в нераспознаваемые
  (12 / 17.00 → 5 / 10.00 и 6 / 10.00 → 13 / 17.00), включение вернуло
  исходные числа. Переназначение: предпросмотр совпал с записанным заранее
  предсказанием до сотой, применение 12 строк, откат «12 возвращено, 0
  пропущено», и ни одна из 10 385 уже привязанных строк не сдвинулась —
  проверено пересечением журнала прогона с контрольной группой. Метки
  областей: оба языка, узбекский кириллицей, неизвестная `Atyrau Region`
  дословно, фильтр по сырому значению; обе книги Excel разобраны `openpyxl`
  и сошлись с экраном (23 вылета / 49.00 га). Предупреждение о нулевом
  регионе найдено в `logs\error.log` с номером партии 66.
  **Не проверено и названо прямо:** правка гонки периода в сборщике — она
  проявляется только против живого кабинета DJI, куда сборщик и ходит;
  защита от повторного применения — из интерфейса невоспроизводима, после
  прохода секция с кнопкой исчезает; переименование ника и смена машины у
  существующего ника — трогали тот же маршрут только галочкой «Активен».

- **2026-07-30 — снято, трек Дронов.** `DRONE-SUMMARY-ALLTIME-001`,
  `DRONE-SUMMARY-XLSX-DIVERGE-001` и `DRONE-SUMMARY-DRILLDOWN-500-001`
  (PR #37, мерж `2e2cccd`; коммиты `f2fab35`, `466cac5`, `c146e46`, `a6fe1e1`
  и `e8eca76` — запись валидации). Проверки прогнаны на staging `2e2cccd`
  **после** мержа: PR смержили раньше приёмки, и строка этого файла, уехавшая
  внутри PR, держала прод-деплой закрытым, пока проверки не закрылись — это
  тот самый «остаток», под который гейт и заведён. Миграций нет, схема и
  данные не менялись, поэтому откат кода является полным откатом.
  Экран за всё время — 10 386 вылетов / 10 167.61 га, «Не распознано»
  1 / 0.00, все четыре разреза сходятся с итогом. Excel с того же экрана
  открыт в настоящем Excel и разобран `openpyxl`: пять листов,
  «Период: с/по — не ограничен», итог `10386 / 10167.6074` на каждом разрезе,
  литры и килограммы не сложены нигде. Машина №8 — 1050 / 1087.76 и на
  экране, и в книге; переход по её строке при применённом фильтре
  (`?date_from=&date_to=&unit_id=8`) открыл список с «Всего: 1050», то есть
  500 вылечен на площадке, а не только на стенде. Узбекский — метка
  «Барча вақт», только кириллица. Права: учётка `test` без права `drones` —
  403 при отрисованном сайдбаре и без пункта меню, обе половины.
  Числа сверены ревьюером независимо по сырым `raw_json`, а не приняты из
  отчёта исполнителя: апрель 6402 / 6379.22, апрель плюс машина №8 —
  648 / 665.76, машина №8 за всё время 1050 / 1087.76 при разбивке
  318 / 533 / 199 по трём написаниям ника. Совпадение апрельских чисел
  доказывает не только чтение данных, но и верную границу суток в UTC+5:
  при неверном смещении апрель разъехался бы.
  Провенанс, зафиксированный как есть: часть тех же чисел ранее измерялась на
  копии staging-базы вне площадки. Здесь они подтверждены на самой площадке;
  измерение на копии остаётся вспомогательным, а не источником истины.

- **2026-07-29 - снято, трек Дронов.** DRONE-004 (PR #36, мерж `ce39593`),
  включая QA-раунд `2322f08`. Проверено на staging `2322f08` **до** мержа.
  Миграций нет, все три новых маршрута read-only, поэтому откат кода =
  полный откат. Сходимость проверена во всех четырёх разрезах: строка
  «Не распознано» 1 вылет / 0.00 га, «Область не определена» 1 / 0.00,
  итог 10 386 и 10 167.61 совпадает с карточками шапки. Все числа сверены
  ревьюером независимо по сырым payload: гектары по 15 машинам, по 6
  областям, по типам работ (10 338 / 48), по 6 месяцам. Оба Excel
  разобраны openpyxl и сходятся с экраном; открываются в Excel штатно.
  Права: 403 на всех трёх маршрутах при отрисованном меню и отсутствующей
  ссылке.

- **2026-07-29 - снято, трек Дронов.** DRONE-000 / DRONE-001 / DRONE-002
  (PR #34, мерж `e6393c8`; PR #35, мерж `5bf2a64`). Проверки прогнаны на
  staging `5bf2a64` **до** мержа, обе строки этого файла сняты тем же
  коммитом, что и запись вехи. Миграция `DRONES_FOUNDATION_001`: реестр
  30 -> 31, повторный прогон `Already applied`, бэкап снят до неё
  (`transport_20260729_173410.db`, 137 748 480 байт, integrity ok).
  Реплей 10 385 сырых вылетов: первый прогон 10385 новых, второй — ноль
  новых при 10 385 дублях (дедуп доказан **дифференциально**, одного
  успешного прогона для этого мало), третий с `--inject-unknown` — вылет
  с неизвестным ником **сохранён** с `drone_unit_id NULL` и счётчиком
  «нераспознан», а не отвергнут. Независимый второй инструмент
  (`verify_drones_replay.py`, сырой `sqlite3` мимо приложения) подтвердил
  суммы, разбивку по шести областям, все пятнадцать помашинных чисел и
  четыре инварианта журнала. Права: учётка `test` без права `drones` —
  403 при отрисованном меню и отсутствующей ссылке, обе половины.

- **2026-07-29 — снято, трек Запчастей.** Инкремент 8 (`SP-CANCEL-008`, PR #29,
  мерж `0827c98`). Все три пункта прогнаны на staging, площадка на `f2572ce`.
  (1) Исключение отменённой заявки из подсказки о повторном заказе (`1a9369a`)
  проверено **дифференциально**, а не по пустому ответу: на паре «техника 34 +
  позиция 97» отменённая заявка 510 от 10.07 лежит внутри 90-дневного окна, и в
  ответе `/spare-parts/api/repeat-check` её нет, а созданный для контроля
  черновик 530 — есть. Положительный контроль на двух других парах вернул все
  ожидаемые id, 7 из 7 и 4 из 4, поэтому «эндпоинт просто молчит» как объяснение
  исключено.
  (2) Карточка отменённой заявки в узбекском: боковая метка стиппера
  `✕ Бекор қилинган`, строка `Сўров бекор қилинган — амал йўқ.`, бейдж
  `Бекор қилинган`, история переходов на узбекском.
  (3) Учётка `test` с `spare_parts_issue = 1` и `spare_parts_approve = 0` на
  заявке 523 видит блок выдачи и **не** видит блока отмены; под администратором
  на той же заявке блок отмены присутствует. Оба контроля нужны: без первого
  «кнопок нет» неотличимо от «карточка не отрисовала действий».
  Права учётки `test` возвращены в исходное состояние и сверены с копией
  `staging\daily\transport_20260729_094318.db` — совпадение по всем модулям.
  Побочный след: черновик 530 на staging — мусор QA, учтён в `DEMO-CLEANUP-002`.

- **2026-07-29 — снято, трек АЗС.** `FUEL-BIG-001` (PR #31, мерж
  `f2572ce`, 52 коммита). Три read-only verify-скрипта прогнаны на копии
  production-базы от 2026-07-28: **23 склада, 0 расхождений** в каждом.
  Миграция `FUEL_COUNTER_MISMATCH` применена на staging 2026-07-29, повторный
  прогон — «Already applied», реестр 28 → 29. Браузерный агент: 41 пункт,
  39 проверено, 1 пропущен обоснованно, 1 дефект — исправлен в QA-раунде.
  Чек-лист владельца закрыт: Excel-выгрузки сверены с эталонным
  прототипом (258 строк, все числа совпали), запись и вход под
  неадминистратором проверены, узбекский просмотрен. Смоук на
  staging после деплоя пройден.
  Остаётся шагом релиза, а не пробелом проверки: применение
  миграции на production — это пункт runbook.
- **2026-07-26 — снято, трек АЗС.** `F1.1 · FUEL-NAV-001` (PR #27, мерж
  `a213874`): два визуальных пункта проверены на staging под администратором —
  строка меню переносится на второй ряд без горизонтальной прокрутки, в русском
  и в узбекском; узбекские метки полные и кириллические. Третий пункт —
  обрезание открытой панели группы на узком экране — закрыт **обоснованием, а не
  прогоном**: панель привязана к `.fuel-nav` через `left: 0; right: 0`, то есть
  по построению равна ширине строки, а `overflow-x` в шаблоне отсутствует,
  проверено чтением исходника.
- **2026-07-25 — снято, трек Запчастей.** Инкремент 7 (`SP-REPORTS-007`,
  PR #17, #18, #19): чек-лист пройден в два прохода — только чтение на
  production 2026-07-24, операции записи и объём на staging 2026-07-25.
  Расхождений нет; единственная находка QA-агента оказалась ложной. Два пункта
  закрыты обоснованием, а не прогоном, и записаны так в `TASKS.md`.
- **2026-07-25 — снято, трек Запчастей.** `add_unit_sqm.py` выполнен на
  production, единица `kv_metr` добавлена.
- **2026-07-24 — снято, трек АЗС.** Заглушка удалена по подтверждению того
  чата: из вех `FUEL-SYNC-013`, `FUEL-MANUAL-EXP`, `-A2`, `-A3`,
  `FUEL-CARD-CLASS`, `FUEL-RESERVE` непроверенного не осталось. Оговорка того
  чата зафиксирована как есть: проверки он унаследовал из промта-передачи, а не
  гонял лично.

Отрицательные остатки, дубли складов и расхождение в счёте складов сюда не
кладутся — это открытые вопросы к данным (`FUEL-NEGATIVE-001`,
`AZS-ORG-REFACTOR` в `TASKS.md`), а не непровалидированный код. Иначе файл
никогда не станет пустым и перестанет работать как гейт.

---

## Приборы с известным шумом

Собственные контроли проекта, которые печатают тревожный вердикт на исправном
дереве. Записаны, чтобы следующий чат не остановил релиз на пустом месте — и
чтобы не привык игнорировать вердикт вообще.

| Прибор | Что печатает | Почему это фон | На что смотреть вместо этого | ID |
|---|---|---|---|---|
| `check_migration_drift.py` | `DRIFT DETECTED` при чистом дереве | 12 файлов без `MIGRATION_ID` и 6 записей без сопоставимого файла — историческая бухгалтерия треков АЗС и ботов | `file-but-not-registered: 0` и `registered migrations` | `TOOL-DRIFT-VERDICT-001` |
| `check_db_lock.py` | `STALE ... DO NOT MIGRATE` при остановленных службах | WAL в проекте персистентен по решению (`sqlite_runtime.py`), `-wal`/`-shm` не удаляются при закрытии | строка `no process holds the database` | `TOOL-DBLOCK-PERSIST-WAL-001` |
| `check_db_lock.py` | называет продовые имена служб при запуске на staging | имена зашиты в сообщение | фактическую площадку, а не текст | `TOOL-DBLOCK-SVCNAME-001` |

Правило ведения: строка добавляется только после того, как шум подтверждён
**дважды на разных площадках** совпавшим выводом. Одного наблюдения мало:
один раз — это находка, а не фон. Строка снимается, когда прибор починен.
