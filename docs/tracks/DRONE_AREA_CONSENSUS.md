# DRONE-AREA — канонический план и реестр утверждений

**Это единственный источник истины по задаче площади дронов.** Ни одна из
сторон не пишет конкурирующий план в другом месте. План меняется только
дельтой в конец файла — с датой, автором и ссылкой на артефакт.

Состояние трека дронов в целом — `docs/tracks/drones.md`. Здесь только
площадь.

Зафиксировано 10.09.2026 по итогам согласования владелец / ChatGPT / Claude
Code. Переговорная фаза закрыта.

---

## 1. Роли

| Сторона | Отвечает | Не делает |
| --- | --- | --- |
| **Владелец** | бизнес-метрика, допустимый tolerance, полевой эксперимент, production/deployment | не арбитр технических споров |
| **ChatGPT** | постановка задачи, критерий доказательности, внешние DJI и нормативные источники, независимый review кода и результатов, попытка опровергнуть | не строит параллельную реализацию |
| **Claude Code** | репозиторий, база, V4, эксперименты, реализация, тесты, evidence bundle, обязательный self-adversarial прогон | не выдумывает бизнес-правила; не финальный ревьюер собственного кода |

**Связь.** Прямого канала между моделями нет. ChatGPT пробовал создать GitHub
Issue и получил `403 Resource not accessible by integration`: чтение
репозитория есть, записи нет. Поэтому канал односторонний — Claude пишет в
репозиторий, ChatGPT читает сам, владелец передаёт только короткий verdict.

## 2. Правила протокола

1. Каждое утверждение имеет тип: **ФАКТ** (обязательна ссылка — `file:line`,
   результат запроса или URL), **ПОДДЕРЖАНО**, **ГИПОТЕЗА**, **ОСЛАБЛЕНО**,
   **ОПРОВЕРГНУТО**, **ОТКРЫТЫЙ ВОПРОС**. Без ссылки ФАКТ не принимается ни
   от кого.
2. Расхождение закрывается артефактом, а не аргументом. Проигрывает сторона,
   у которой артефакта нет, независимо от убедительности.
3. **Ни одна сторона не переводит собственное утверждение в ДОКАЗАНО.** Для
   этого нужен заранее сформулированный тест на опровержение, который был
   проведён и не опроверг, плюс независимое подтверждение. «Возражений не
   нашли» — это ПОДДЕРЖАНО, а не ДОКАЗАНО.
4. Один активный макроэтап. Пока он не закрыт, новых планов не пишет никто.

## 3. Разные величины, которые нельзя смешивать

| Величина | Что это | Когда известна |
| --- | --- | --- |
| `FIELD_AREA` / `totalArea` | геометрия нарисованного контура | всегда |
| `TASK_AREA` / каталожный `workArea` | плановая рабочая область после исключений | **до вылета** |
| `DJI_RAW_RECORDED_AREA` / `new_work_area` | что записал DJI | после вылета |
| `DJI_CORRECTED_RECORDED_AREA` | техническая коррекция записи DJI | после расчёта |
| `V4_APPLICATION_COVERAGE_ESTIMATE` | уникальная поверхность под подтверждённым применением | цель этапа 1 |
| `VERIFIED_UNIQUE_APPLICATION_AREA` | то же после физической калибровки и holdout | цель этапа 3 |
| `BILLABLE_AREA` | договорное число | отдельным решением владельца |

Правила, принятые обеими сторонами:

- `Task Area` — **плановая** величина. Официально: «displays the value of the
  actual area of the **planned task route after planning the field**»
  (руководство Agras T30 v1.4). Она не меняется, если дрон обработал часть
  поля, и полностью задаётся оператором при планировании (граница,
  препятствия, отступ). Биллинг по ней — оплата за рисование.
- `Obstacle Area` — это **препятствия внутри контура**, а не холостые
  перелёты. Вычитание её холостой ход не убирает.
- Фантомная запись **не вычитается**. Она теряет право быть источником
  истины, а вклад интервала восстанавливается независимо по V4.
- **Прямая линия ≠ холостой полёт.** Правило `gap_m = 60 м` уже стоило
  проекту неверного результата: 103 из 103 длинных прямых отрезков внутри
  контура оказались настоящими проходами (`docs/DJI_AREA_48H_DECISION.md` §10,
  коммит `a5dc4e6`). Для точек маршрута (упрощённая ломаная) разрыва по
  расстоянию нет вовсе. Для кадров V4 — телеметрия ~1 Гц — разрыв есть, и он
  проверяется временем и физической скоростью, а не длиной.

## 4. План: три макроэтапа

**Этап 1 — FIRST INDEPENDENT V4 APPLICATION COVERAGE.** Блок 1A — код в
чистом репозитории. Блок 1B — прогон на реальных данных сервера. Результат:
первая воспроизводимая карта и число для дронов №5 и №6 за 18.08.2026.
Называется `V4_APPLICATION_COVERAGE_ESTIMATE`, не billable.

**Этап 2 — физическая калибровка.** Один участок, независимо измеренная
геометрия, водочувствительные карточки по методике NY/T 3213—2023 (граница
полосы по плотности не менее 20 капель/см², шаг не более 0.2 м, не менее трёх
повторов). Специально созданные: нормальный проход, перекрытие, край поля,
пауза/возобновление, дозаправка, перелёт без распыления, ручное распыление и
**намеренно завышенное междурядье**.

Последнее — не про точность, а про antifraud. Междурядье задаёт оператор, и
площадь линейно от него зависит; при завышении дозатор льёт пропорционально
шире, поэтому расход на гектар остаётся константой и контроль «литры на га»
манипуляцию не видит. Ловят её только карточки.

**Этап 3 — заморозка, слепой holdout, продукт.** Версия и параметры
фиксируются. Невиданный период, несколько аппаратов, выборка и прогноз
фиксируются до вскрытия, подгонки после просмотра нет. Отдельно оцениваются
нормальные случаи, дубли/stale, ручной режим и возобновление, недостаточное
evidence. Только после этого — паспорт работы, очередь исключений и отдельным
решением правило `BILLABLE_AREA`.

## 5. Реестр утверждений

| ID | Утверждение | Статус | Артефакт |
| --- | --- | --- | --- |
| A1 | Дневной итог DJI по машине = сумма `new_work_area` её записей за отчётный день (UTC+5) | **ФАКТ** | №6: 36 записей, 28.7469 га против 28.75 на экране; №5: 47 записей, 36.3977 против 36.4 |
| A2 | Зелёный пузырь на карте iOS = сумма `new_work_area` по географической группе вылетов, а не независимое измерение | **ПОДДЕРЖАНО** | `Kamoliddin omad` = 9.7142 ↔ 9.71 и `AliMirzozamini` = 10.4276 ↔ 10.43 — совпадение с нулём степеней свободы; разбиение по времени: №6 p < 0.0025, №5 p ≈ 0.010 на 400 контролях с той же суммой |
| A3 | Подпись `X.XX ha` под именем контура — геометрия участка, а не выполненная работа | **ПОДДЕРЖАНО** | подпись стоит на белых (необработанных) контурах: `image3`, `image4`, `image7` |
| A4 | Какое именно поле DJI печатает подпись iOS (`totalArea`, `workArea` или `totalObstacleArea`) | **ОТКРЫТЫЙ ВОПРОС** | `dji_area/evidence.py:355-373` — узел контура несёт все три; никто не проверял, какое печатается |
| A5 | Каталожный `workArea` = Task Area конкретной миссии | **ОТКРЫТЫЙ ВОПРОС** | `workArea` — свойство участка в каталоге; у миссии свои настройки отступа и препятствий; записей о миссиях не собираем |
| A6 | Четыре записи `COUNTER_FLAT_RAW_OVERSTATED` дрона №5 (3.5592 га) объясняют, почему картина №5 отличается от №6 | **ФАКТ (записи), ОСЛАБЛЕНО (объяснение)** | 1B: пять независимых линий против этих четырёх -- плоский счётчик, точный повтор скаляра, физика (12.0-18.7 м/с при паспортных ~7), `application_frames = 0` при канале INFORMATIVE, `TIER5_UNKNOWN`. НО у №6 есть кандидат с той же подписью (`688472022`, 14.7 м/с, повтор `688472018`, 0.0 м хода), который НЕ понижен из-за `V4_WINDOW_INCOMPLETE`. Значит разница бортов частично объясняется доступностью улик, а не поведением машин |
| A7 | Снятие фантомов закрывает расхождение с суммой контуров | **ОПРОВЕРГНУТО** | 26.68 − 3.5592 = 23.12 против 24.97: остаток не уменьшился, а сменил знак (+1.71 → −1.85) |
| A8 | `new_work_area` — километражный интеграл, удваивающий перекрытие | **ОСЛАБЛЕНО** | 4 из 4 оппонентов: на штатной маршрутной работе интеграл по междурядью и объединение численно неразличимы; учётная ширина = междурядье, поэтому боковое перекрытие не удваивается |
| A9 | `new_work_area` = приросту счётчика V4 там, где счётчик растёт | **ФАКТ** | 12 записей 18.08 с ненулевой дельтой: остаток — точное кратное 2/3 м² (0.001 му), максимум 34 м² на 8586 |
| A10 | Природа счётчика V4: маршрутный накопитель по междурядью либо дедуплицированное покрытие | **ОТКРЫТЫЙ ВОПРОС (измерено, не закрыто)** | 1B: **0 различающих случаев из 83**. S/U у №5 медиана 0.9815 макс 1.0693, у №6 медиана 0.9843 макс 1.0094 -- распределение прижато к единице. Это настоящий отрицательный результат, а не сбой: на штатной параллельной работе две гипотезы численно неразличимы (подтверждает A8). Нужен день с реальным перекрытием -- возобновление после долива, повторная обработка |
| A11 | Сумма подписей `ha`, прочитанных с карты, — корректная база сравнения | **ОСЛАБЛЕНО** | контуры перекрываются и вложены: на `image3` не менее пяти полигонов с именем `ShukrulloBerdizamini` поверх одной земли; `TIER1_EXACT` = 0 для всех 226 записей |
| A12 | Записанная `spray_width` соответствует физической полосе осаждения | **ОТКРЫТЫЙ ВОПРОС** | NY/T 3213—2023 определяет ширину по плотности не менее 20 капель/см²; проверяется только этапом 2. **1B: ограничение касается ВСЕХ его чисел** -- и S, и U масштабируются вместе с записанной шириной |
| D1 | Модель считала от изменяемой `drone_flights.raw_json`, а не от захешированной ревизии | **ФАКТ, ИСПРАВЛЕНО ЧАСТИЧНО** | было `dji_area/pipeline.py:100-102`. Pipeline больше не разбирает изменяемую колонку и различает три состояния источника. НО см. A13: на живых данных ветка ревизии не срабатывает вовсе |
| A13 | Неизменяемая цепочка источников разорвана в первом звене: коллектор НЕ захватывает ответ списка вылетов как источник | **ФАКТ, ИСПРАВЛЕНО (не проверено живьём)** | было: `drone_collector/main.py` слал только `card`/`route`/`airlines`/`v4`; 1B подтвердил на живых данных -- `LIST_FROM_MUTABLE_RAW_JSON` у 83 из 83. DRONE-AREA-CAPTURE-001: `FlightCollector` теперь сохраняет СЫРЫЕ байты ответа `/api/web/v1/flight_records` и кладёт страницу как `list`-ревизию через очередь; у каждого вылета страницы своя ревизия на одно тело, запись выбирается по `id`. Изменяемый `raw_json` остаётся ТОЛЬКО помеченным запасным путём для исторических вылетов -- backfill не делается. Доказано синтетикой (61+22 теста), на живом DJI НЕ проверено |
| A14 | Отпечаток входа отслеживает СТРОКУ `V4_PARSER_VERSION`, а не содержимое парсера | **ФАКТ** | правка карты полей `dji_area/v4.py` без подъёма константы даёт тот же hash; `pipeline.py:210` вдобавок отдаёт кэш сводки по той же константе. Ничто не связывает объявленную версию с кодом; копий константы три (`__init__.py:52`, `drone_collector/sources.py:116`, литерал в `tools/dji_area_import_sources.py:218`) |
| A15 | Причина и величина разности S и U | **НАБЛЮДЕНИЕ ЗАФИКСИРОВАНО, ПРИЧИНА ОТКРЫТА** | 1B, наблюдение: разность S и U систематически отрицательна -- у 59 из 76, сумма -0.7397 га, медиана доли **-1.66 %**; это заметно больше измеренной чувствительности к шагу сетки (медиана 0.051 %, p90 0.19 %). Следует ТОЛЬКО одно: разность не объясняется шагом сетки, поэтому прежний порог `длина x шаг сетки` снят правильно. НЕ следует и остаётся UNKNOWN: причина знака и величины (растровое объединение целых ячеек -- правдоподобно, но не проверено; вклад могут давать геометрия перекрытий и сшивка сегментов) и абсолютное смещение U относительно земли -- медиана -1.66 % это разность ДВУХ ОЦЕНОК, а не ошибка одной. Корректирующий коэффициент не вводится |
| A16 | Точный повтор скаляра сам по себе НЕ является признаком фантома | **ФАКТ** | 1B: пара №6 `689306078` / `689313066`, оба 9946 м², 2690 и 2788 кадров применения, 1563.2 и 1554.2 м хода, физика не нарушена. Правило «точный повтор = фантом» уничтожило бы 0.9946 га реальной работы |
| A17 | Историческая геометрия недоступна не по случайности, а потому что тела геометрии не захвачены | **ФАКТ** | 1B: 83/83 `contour_status = CONTOUR_ABSENT`; **20/20 ревизий земель имеют `status = NOT_CAPTURED`**. Тот же класс, что A13. Разбиение «внутри контура / вынос» не посчитано ни разу и посчитано быть не могло |
| A18 | V4-реконструкция согласуется с записанным счётчиком DJI | **ПОДДЕРЖАНО как согласие ДВУХ ТЕХНИЧЕСКИХ МЕТРИК** | 1B, на сопоставимых вылетах: №5 U = 33.0234 против DJI corrected recorded 32.8385 -- **+0.56 %**; №6 U = 29.0110 против 28.3696 -- **+2.26 %**; по участкам 13 из 20 в пределах ±5 % от каталожного `work_area`. Это НЕ точность по физической площади: земля как ground truth не измерялась, а обе величины опираются на одну и ту же записанную бортом ширину (A12 открыт) -- систематическая ошибка ширины сдвинула бы обе метрики одинаково и здесь не проявилась бы. Поправку на четыре фантома №5 подтверждает; числом к счёту не является |
| A19 | Обоснование A13 «`raw_json` переписывает любой повторный приём» было НЕВЕРНЫМ | **ОПРОВЕРГНУТО (собственное утверждение)** | найдено состязательным чтением: `api_flight_sync` пропускает дубликаты (`drones.py:578`) и пишет `raw_json` только при вставке (`drones.py:630`); других писателей в приложении нет, перезапись живёт в `reference/dji_collector_2026/` против другой таблицы и другой базы. Настоящая причина слабее по громкости и не слабее по сути: колонка не покрыта SHA, правится любым SQL/миграцией/`init_data.py` и хранит НАШУ копию строки, а не ответ DJI. Комментарий в `pipeline.py` исправлен |
| A20 | Пропуск известного полигона и ПОТЕРЯ полигона снаружи неотличимы | **ФАКТ, ЗАКРЫТО** | найдено состязательным чтением приёмника: ссылка без тела не роняет приём, а тихо понижает привязку поля с `TIER1_EXACT` до `TIER2_STRONG`, сохраняя HIGH-уверенность. Приёмник теперь возвращает `geometries_referenced_but_absent`; 0 -- корректный пропуск, ненулевое -- потеря |
| A21 | Валидация геометрии (GeoJSON, кольца, сверка площади) на неизменяемом пути НЕ выполняется | **ФАКТ, ОТКРЫТО** | `GeometryRun` (`geometry.py:588`) всё это делает и кладёт в очередь `KIND_FIELD_GEOMETRY`, которую НИКТО не дренажит -- тупик. Путь, который реально доезжает до хранилища (`download_snapshot_geometries`), проверяет только размер, md5 и маркеры секретов. Разбор происходит на приёмнике, но приёмник вызывает упрощённый `field.rings_from_geojson`: он достаёт кольца и НЕ проверяет ни замкнутость, ни самопересечение, и площадь не считает. Значит настоящий валидатор по сохранённым полигонам не проходил ни разу. Цена измерена живым прогоном 12.09.2026: из 6210 сохранённых полигонов **786 (12.7%) отвергает валидатор годности колец**, 2543 кольца самопересекаются; `unreadable=0`, `no_plant=0` -- то есть JSON и PlantZone есть у всех, падает именно годность, и приёмник все 786 принял не проверяя. С DJI-AREA-POLYGON-AUDIT-001 (`tools/dji_polygon_audit_001.py`) валидатор можно прогнать ЧИТАЯ: аудит считает площадь колец и сверяет её с объявленной DJI. Долг закрывает не аудит, а перенос валидации на путь записи; аудит лишь показывает цену вопроса |
| A22 | Два РАЗНЫХ захвата одного вылета в одном окне разводятся молча, по id ревизии | **ФАКТ, ОТКРЫТО** | найдено состязательным ревью. Внутри ОДНОЙ страницы две различающиеся записи одного вылета -- отказ (`evidence.select_list_record`). Между РАЗНЫМИ страницами одного окна такой защиты нет: сайт может перезапросить страницу на середине обхода (`browser.py` считает это `self_duplicates`), и если DJI за это время обновил ещё догружавшийся вылет, получатся две list-ревизии с одинаковым `captured_at_utc`; `latest_revisions` сортирует по `captured_at_utc, id`, то есть побеждает последняя по id, и строка всё равно помечается `LIST_FROM_REVISION`. Обе ревизии неизменяемы и сохранены, потери нет -- вопрос в том, что выбор между ними не объявлен. Не чинится в этом инкременте сознательно: правильное решение требует понимания, какой захват DJI считает окончательным, а это данные, которых у нас нет |
| A23 | Контур поля пригоден как ФИЗИЧЕСКАЯ эталонная ссылка для polygon closure | **ОПРОВЕРГНУТО** | Живой прогон 12.09.2026 (6210 полигонов): расхождение площади колец с объявленной DJI `totalArea` -- медиана 0.2242%, p99 0.2259%, максимум 0.2675%, знаковая медиана равна медиане модуля. На 5423 полях медиана и p90 расходятся на 0.0002 пп. Два независимых измерения так не совпадают; так совпадает одна формула по одному полигону. Значит `totalArea` ПРОИЗВОДНА от полигона, и сверка с контуром проверяет согласие нашего интегрирования пути с выданным полигоном, а не границы настоящего поля. Полосу приёмки closure этим эталоном задать нельзя. Что closure даёт реально -- обнаружение грубых ошибок (недолёт, вынос за контур, двойной счёт перекрытий) |
| A24 | Наша площадь считается на том же радиусе Земли, что у DJI | **ОПРОВЕРГНУТО, ИСПРАВЛЕНИЕ НЕ ВНЕСЕНО** | `ring_area_m2` (`drone_collector/geometry.py:158`) берёт `EARTH_RADIUS_M = 6378137` -- большую полуось WGS84. DJI считает на сфере **6371000**: `(6378137/6371000)^2 - 1 = 0.2242%`, совпадение с измеренной медианой до четвёртого знака. Утверждение узкое и шире не заявляется: **ГЕОМЕТРИЧЕСКИЕ площади, посчитанные трактом через `ring_area_m2`, несут систематический масштабный сдвиг примерно +0.2242% относительно геометрии, посчитанной на DJI-совместимой сфере 6371000**. Это свойство одной формулы, а не всей системы. Из него НЕ следует: что надо корректировать скалярную площадь DJI; что надо корректировать счётчик V4 (поле 9); что надо корректировать любую vendor-recorded площадь; и что все площади системы завышены на 0.2242%. Смещение относится ровно к тем числам, которые мы считаем сами из колец. Falsification: подставить 6371000 и убедиться, что медиана расхождения площади колец с `totalArea` садится к нулю |
| A25 | `FIELD polygon` и catalog `workArea` существенно расходятся | **ФАКТ, ИЗМЕРЕНО** | Тот же прогон: в исследованной выборке catalog `workArea` меньше площади колец `FIELD polygon` на медиану **7.3751%** (медиана модуля 7.5591%, p90 19.86%, p99 69.06%, максимум 1390.65%; в пределах 5% лишь 1424 из 5291). Измерено РОВНО это и ничего сверх: сравнивались площадь колец полигона и объявленное каталогом число `workArea`. **`catalog workArea` НЕ является доказанным mission `TASK_AREA`**: происхождение, семантика и соответствие `workArea` реальной области задания миссии остаются OPEN, и называть эту разницу «задание уже поля» оснований нет. Практический вывод, который из измерения СЛЕДУЕТ: сверять покрытие с полным контуром поля -- не то же самое, что сверять его с `workArea`, и выбор базы сравнения надо объявлять явно. Вопрос оплаты внесения внутри поля за пределами того, что каталог считает `workArea`, остаётся бизнес-правилом владельца. Falsification: найти в живом ответе DJI поле или документ миссии, задающие область задания, и сверить их с `workArea` |
| D2 | Отпечаток входа не покрывал `V4_PARSER_VERSION` | **ФАКТ, ИСПРАВЛЕНО** | было `dji_area/hashing.py:38-54`; `__init__.py:36-38` сам объясняет механизм, на который опирался |
| D3 | Запись с наблюдённым применением при плоском счётчике попадала в ПРОВЕРЕННЫЙ подытог с жёстким нулём | **ФАКТ, ИСПРАВЛЕНО** | было `resolver.py:71` + `486-492`; статус не сломан, изменено только право на агрегирование |

## 6. Отложенный трек (не начинать)

**`docs/RELEASE_GATE.md` — 16 открытых пунктов, все «Дроны», прод-деплой
закрыт всему холдингу.** Больше половины к площади отношения не имеет.
Разбирается **отдельной задачей после текущего макроэтапа** — по прямому
решению владельца от 10.09.2026. В рамках задачи площади не трогать.

## 7. Дельты

*(сюда дописываются изменения плана: дата, автор, артефакт, что изменилось)*

- **10.09.2026, Claude Code, self-adversarial прогон.** 15 агентов-оппонентов
  на 5 утверждений: F1 (D1) и F2 (D2) получили по два ОПРОВЕРГНУТО, F3
  ОСЛАБЛЕНО. Найдено и исправлено: ветка ревизии срабатывала по одному
  `list_revision_id`, даже когда все скаляры NULL (третье состояние
  `LIST_REVISION_UNPARSED`); `repeated_application_ha` уходил в минус,
  потому что S аналитический, а U растровый (теперь отдельно `s_minus_u_ha`
  и оценка растрового шума, «повтором» называется только превышение над
  шумом); два комментария `[REASON]` утверждали механизмы, которых нет.
  Выявлено и НЕ закрыто: A13 (коллектор не шлёт `list`) и A14 (версия
  парсера — необеспеченная строка). Провенанс-фикстура расходилась лишь в
  2 скалярах из 6 — теперь во всех шести. Мутационный прогон: `max` вместо
  `min` для ширины, пороги 9.9 с и 99 м/с, снятый фильтр версии алгоритма и
  две «половинчатые» правки D1 — все убиты тестами.

- **11.09.2026, Claude Code, DRONE-AREA-CAPTURE-001 (макроэтап).**
  Инфраструктура захвата доказательств перед multi-date validation.
  MACRO A: ежедневный снимок каталога стал инкрементальным -- `contentMd5`
  известен из узла каталога ДО скачивания, поэтому коллектор спрашивает
  приёмник read-only манифестом, какие полигоны уже лежат, и качает только
  новые. Бенчмарк брифа доказан фикстурой: 6171 известных + 13 новых = 13
  загрузок, и проверяется по тому, что подписанную ссылку никто не
  запросил. Недоступный манифест деградирует в полную загрузку, а не в
  снимок без полигонов. MACRO B: закрыт A13 -- сырые байты ответа списка
  становятся неизменяемой ревизией через очередь.
  Три находки состязательного чтения шести подсистем, все исправлены:
  (1) первая редакция отправки списка МИНОВАЛА очередь, теряя атомарность,
  дедуп, гейт секретов и досылку -- переведена на штатный путь;
  (2) «пропущено, потому что уже есть» и «тела нет» были снаружи
  неотличимы -- приёмник теперь возвращает
  `geometries_referenced_but_absent` (A20);
  (3) собственное обоснование A13 оказалось фактически неверным (A19).
  Второй круг ревью (три объектива, состязательная проверка находок) нашёл
  ещё четыре дефекта, все исправлены: тревога
  `geometries_referenced_but_absent` вычислялась приёмником и МОЛЧА
  выбрасывалась сборщиком; `api_status` живых list-ревизий всегда был NULL
  (`body_code` получал разобранный dict вместо байтов); счётчики списка
  присваивались вместо накопления и у периода через границу года сводка
  показывала только последнее окно; сама тревога считалась по ВСЕМ
  ревизиям за всё время, поэтому один невосстановимый пробел прижимал её
  выше нуля навсегда -- сужена до текущих ревизий.
  Зафиксированы долги A21 (валидация геометрии живёт на пути, который
  никуда не отправляет) и A22 (два разных захвата одного вылета в одном
  окне разводятся молча).
  Ничего из этого НЕ проверено на живом DJI; миграций и новых таблиц нет.

- **11.09.2026, владелец (прогон) + Claude Code (разбор), этап 1B выполнен.**
  Block 1B отработал на площадке, production не участвовал. Результаты и
  ограничения -- `docs/area-evidence/BLOCK1B_RESULTS.md`, таблицы --
  `docs/area-evidence/block1b-2026-08-18/`. Все заявленные числа сошлись:
  №5 RAW 36.3977 / плоские 3.5592 / corrected 32.8385 (разность точная),
  №6 RAW 28.7469, 83/83 без доказанной геометрии, 0 различающих случаев.
  Ворота идемпотентности отработали на живых данных: `flights_in_period 226`,
  `calc_writes {"unchanged": 226}` -- это первый прогон после правки `4f7147f`,
  до неё контроль был мёртв. Хеши модулей в манифесте сошлись только после
  LF -> CRLF (рабочая копия Windows), код тот же.
  Реестр обновлён: A6 ослаблен (у №6 есть кандидат с той же подписью,
  не пойманный из-за `V4_WINDOW_INCOMPLETE` -- разница бортов частично
  объясняется доступностью улик), A10 остаётся открытым с измерением,
  A15 зафиксирован как наблюдение с ОТКРЫТОЙ причиной (разность S и U
  больше чувствительности к шагу сетки -- это всё, что доказано; причина и
  абсолютное смещение U остаются UNKNOWN, корректирующий коэффициент не
  вводится), добавлены A16 (точный повтор не признак фантома, живой
  контрпример на 0.9946 га), A17 (20/20 геометрий NOT_CAPTURED) и
  A18 (V4 согласуется с записанным счётчиком: +0.56 % у №5, +2.26 % у №6 --
  согласие двух технических метрик, не точность по физической площади).
  A12 (физическая ширина) и A4/A5 (каталожные поля) не сдвинулись.

- **10.09.2026, Claude Code, второй self-adversarial прогон (15 агентов).**
  Пересмотрено собственное решение предыдущего прогона. Оценка растрового
  шума `длина x шаг сетки`, которой «повтор» отделялся от шума, ОПРОВЕРГНУТА:
  оппонент измерил, что она в десятки раз больше настоящей ошибки растра и
  ГЛУШИТ реальное перекрытие — 0.23 га двойной обработки отдавались как
  `None`, — а сама формула тождественна подобранному правилу
  `S/U > 1 + cell/w`. Порог снят: отдаются `s_minus_u_ha`, положительная
  часть, доля от U и `method_note`; решение принимает читатель, потому что
  в опыте 1B разница между шумом и повтором — это 0.2 % против 100 %.
  Ещё исправлено: `flown_swath_ha` был объединением ВСЕГО закрашенного,
  включая работу, а подписан как холостой пролёт (переименован в
  `painted_swath_total_ha`, холостая часть — отдельным
  `flown_not_applied_ha` и только на одной сетке); причина
  `NO_APPLICATION_FRAMES` была жёсткой и врала, когда распыление шло, а
  полоса не строилась из-за отсутствующей ширины (теперь выводится из
  преобладающей причины сегментов); `or 0.0` превращал «не измерено» в
  ноль; запас без отметки времени 60 м оказался ровно `gap_m` из v1 —
  правилом, от которого модуль и уходит (сужен до 20 м); погрешность,
  которую движок МЕРИТ вторым прогоном на грубой сетке, экспортёр ронял.
  Один сбойный вылет больше не роняет весь bundle: `FAILED` с текстом
  ошибки в `coverage.csv` плюс WARNING в сводке — bundle собирается один
  раз, руками владельца, на недостижимой из сессии машине.
  Мутационный прогон, 10 мутантов: `or`→`and` в правиле применения,
  `dt<=0`→`dt<0`, возврат запаса к 60 м, снятые проверки ширины,
  `any`→`all` в провенансе, снятый фильтр версии резолвера, обмен колонок
  work/obstacle, порог S/U на 1.02 и на 1.50, снятый перехват нештатной
  ошибки и молчаливый FAILED — все убиты. Порог `DISCRIMINATING_S_OVER_U`
  зажат в окно [1.111, 1.335]; точнее не пинуется намеренно — это
  инженерное суждение, и тест на 1.149 против 1.151 запрещал бы его
  осмысленную правку.

- **10.09.2026, ChatGPT (review) + Claude Code (правка), exporter 1B.**
  Consolidated review нашёл в инструменте выгрузки шесть замечаний, два
  блокирующих; оба подтверждены на закоммиченном коде и исправлены.
  (1) Сводка V4 бралась связью по `flight_id`, который в
  `dji_v4_summaries` НЕ уникален — уникален `source_revision_id`. Теперь
  `v.id = c.v4_summary_id`, то есть та сводка, которую назвал сам расчёт.
  Отрицательный контроль показал, что прежняя связь действительно давала
  дубль строки. (2) Историческая идентичность контура не выгружалась вовсе,
  и брались все ревизии по `land_uuid` — прямое нарушение пункта A11.
  Теперь выгружаются `land_snapshot_id`, `land_revision_id`,
  `historical_geometry_sha256`, ревизия атрибуции помечена
  `USED_BY_ATTRIBUTION` отдельно от прочих, а при недоказанной исторической
  геометрии вывод — `UNKNOWN_CURRENT_SNAPSHOT_ONLY`. (3) Bundle стал
  самодостаточным: неизменяемые тела V4, маршрутов и геометрии полей
  выгружаются с проверкой SHA/MD5 и манифестом, расчёт воспроизводится без
  доступа к серверу. (4) `total − obstacle − work` вынесен как
  `unexplained_exclusion_ha` и НЕ называется safety margin. (5) Все выборки
  идут в одной read-транзакции; непустой каталог вывода останавливает
  прогон. (6) `implied_speed` переименована в `min_required_speed_m_s`:
  знаменатель включает время без обработки, поэтому величина
  фальсифицирует запись только сверху и ничего не подтверждает снизу.
  Замечание про `S` относилось к черновику из scratchpad: в
  `dji_area/coverage.py` S с самого начала считается как
  `сумма(ширина_i × расстояние_i)` по кадрам, а не через `width_max`.

- **10.09.2026, Claude Code, этап 1A.** Файл создан. D1/D2/D3 подтверждены
  аудитом и исправлены, версия алгоритма поднята до `impl-3`. Добавлен
  `dji_area/coverage.py`: работой становится отрезок с наблюдённым
  применением из кадра V4, геометрия считается существующим ядром
  `useful-area-v2` без изменений в нём. Новый движок не создавался.
