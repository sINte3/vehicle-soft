# Трек: GPS план-факт

> Handoff по формату запроса от 2026-07-29. Для `docs/tracks/gps-plan-fakt.md`.

## 1. Цель и границы трека

Измерять факт выполнения работ по треку GPS: обработанную площадь в гектарах за произвольный интервал по паре
«техника + контур», чтобы закрывать наряд и выставлять счёт контрагенту защитимой цифрой. Стратегическая рамка
— нишу против agro-work.uz держит не GPS, а связка гектара с фактической выдачей топлива Topaz.

**Не входит:** агрономия (культуры, севооборот, нормы), планирование работ, ввод нарядов как таковой, рейсовые
перевозки (отдельная задача, только эскиз), переписывание справочника контуров в Wialon, любые записи в
Wialon.

## 2. Ключевые архитектурные факты

Источник — реальные ответы Wialon Remote API и расчёты по ним, не отчёты агентов.

- Wialon **Local**, `https://web.gpstrack.uz` → **195.158.11.71**, публичный адрес, сервер интегратора, **не в
  сети кластера**. Внутренний DNS `10.103.25.3` отдаёт тот же публичный IP. Версия ~2504 — со слов владельца,
  лично не проверял.
- Пользователь «Бухоро агрокластер» `id=284`, родительский аккаунт `169`.
- Ресурсы: `285` — **17 812 геозон**; `170` «Дилер 1» — **0 геозон**. Контуры наши, зависимости от дилера в
  них нет.
- Объектов видно **482**; наш реестр техники ~340, разница — легковые, погрузчики, экскаваторы, комбайны.
- **IMEI (`uid`) базовым флагом не отдаётся.** Постоянный ключ сопоставления — `wialon_id`. Переименования в
  Wialon его не ломают.
- Код региона присутствует только в **189 именах из 482**; 6 номеров указывают на несколько объектов; «New
  Holland 80 605 EA» существует тремя объектами (`10077`, `10011`, `2982`) — история замены трекеров.
  Автоподбор по номеру — предложение, а не источник истины.
- **Контур по имени определять нельзя.** «1508 Нурхон Бобохон» — две разные зоны (`3207` 4,32 га и `3208` 8,97
  га); у одного контрагента ~150 зон, повторов номера минимум четыре. Контур определяется **по треку**: какие
  полигоны содержат точки в рабочем режиме.
- Датчики на 16 проверенных тракторах: топливо `[LLS1]+[LLS2]` (`io_201`, `io_203`), зажигание `io_239` как
  моточасы, одометр `io_199`. **ВОМ нет.** У 10 единиц есть «Прицеп» на `io_78` — это вход iButton; стоят ли
  считыватели физически, не проверено.
- Трекеры Teltonika FMB125/FMB920: в движении 30 с / 100 м / поворот 10° / скачок 10 км/ч, отправка 60 с,
  стоянка 30 мин. Фактическая плотность в работе **14–28 с на точку** — триггер по повороту сгущает точки на
  разворотах, то есть на границе полигона.
- Метод расчёта площади (зафиксирован, проверен): точки в окне **1–15 км/ч** и внутри контура → сгущение пути
  каждые **5 м** → альфа-форма по Делоне с **альфа = 10 м** → объединение → обрезка по контуру → площадь в
  **UTM 41N** (EPSG:32641). Реализация: `shapely`, `scipy.spatial.Delaunay`, `pyproj`.
- **Ширина захвата агрегата не используется нигде.** Шаг между проходами измеряется из самого трека: 5–9 м у
  культиватора, 13–14 м у опрыскивателя.
- Результат на проверочном наборе 27.07 (17 работ, метод объявлен письменно до получения данных): медиана
  расхождения с ручным замером **3,3%**, среднее −4,0%; сумма 51,71 га расчёта против 53,09 га ручных.
- Проверка проекции: площадь контура 2714 из геометрии Wialon **42,30 га** против паспортных 42,16 га
  (+0,33%), цифры получены разными путями.
- Правило начисления при совместной работе — **сумма по единицам техники** (2714: 6,792 + 6,933 га за день),
  со страховкой: считать рядом объединение полигонов и помечать пересечение, если сумма его превысила.

## 3. Сделанные инкременты

| Код | Что сделано (одна строка) | PR | Мерж-коммит |
|---|---|---|---|
| — | Кодовых инкрементов нет | — | — |

Трек исследовательский и намеренно не трогал репозиторий: ни коммитов, ни PR, ни миграций, ни строк в
`RELEASE_GATE.md`, `AGENT_STATE.md`, `TASKS.md`. Гейт по этому треку пуст. Этот файл — первая запись трека в
репозитории.

## 4. Открытые хвосты

- Расхождение +23% по контуру «8852-3 галла 2026» (расчёт 1,35 га против ручных 1,094) не диагностировано.
  Проверить: открыть трек `3068` за 23.07 в Wialon и глазами определить, обвёл ли владелец меньшую область или
  алгоритм принял проезд за работу.
- Правило **`альфа = max(10; 1,2 × измеренный шаг)`** даёт по опрыскиванию −1,0% вместо −8,6%, но **подобрано
  на тех же данных** и результатом не считается. Проверить: новый набор из 10–12 ручных замеров с долей
  опрыскивания не менее трети, расчёт до раскрытия замеров.
- Объект `353` (МТЗ 291 HA) дал **0 сообщений** 19.07 при живом тракторе. Проверить: запросить сообщения за
  соседние даты, при пустоте — трекер.
- Смысл битов маски прав токена `1785153493` не расшифрован по документации SDK; установлено только, что 18
  бит из 32 и что это не read-only.
- Найдено 0,87 га работы на контуре «4305 Бобуршох» (МТЗ 167 КА, 19.07), которых нет в ручном учёте. Проверить
  у владельца, была ли эта работа.
- Протокол изучения agro-work под Cowork **не написан**, доступ не использовался.

## 5. Следующие шаги трека

> 2026-08-12: видение, план работ и дорожная карта детализированы в
> `docs/GPS_PLAN_FAKT_VISION_ROADMAP.md`; метод площади воспроизведён
> независимо по письменному описанию (`tools/gps_area_method_repro.py`,
> все контроли PASS). Открытые вопросы владельцу — там, раздел 9.

1. Добор калибровки по опрыскиванию: 10–12 новых ручных замеров (раздел 7), расчёт — до раскрытия
   замеров. Адаптивная альфа `max(10; 1,2 × шаг)` в метод **не вводится** — зафиксировано в
   `docs/PLAN.md` §5 (редакция 2026-07-29): она подобрана на тех же данных, на которых измерена.
   С замороженным правилом альфы недостающие замеры гейтируют **подтверждение точности по
   опрыскиванию**, а не написание ТЗ.
2. Определение контура по треку оформить как обязательный механизм, вместе с флагом качества расчёта (число
   точек, разрывы **в движении**, потерянное время).
3. Сопоставление ~340 единиц ↔ `wialon_id` с колонкой «объект живой». **Зависит от ручной работы владельца**,
   см. раздел 7.
4. Архитектура приёма: пулл по API (не ретранслятор — у нашего сервера нет публичного адреса), хранилище точек
   **отдельно** от `instance\transport.db` (~1 млн точек в сутки, ~30 млн в месяц), в основную БД только
   суточные агрегаты.
5. Зеркалирование контуров в нашу БД — справочник не должен зависеть от чужого хостинга.
6. Связка «наряд → контур → факт площади → закрытие → счёт» с полигоном как приложением к счёту, затем
   себестоимость гектара с топливом Topaz. **Зависит от модуля АЗС** в части фактической выдачи.
7. Рейсовые перевозки: визиты в геозоны, необъяснённый пробег как главная метрика, топливная невязка вторым
   контуром. Только эскиз, к проработке не приступали.

## 6. Грабли этого трека

- **Пустой ответ `core/search_items` — не отказ в правах.** Поиск юнитов по списку id вернул 0 объектов из-за
  неверной спецификации; было принято за отсутствие прав. Работает `core/search_item` по одному объекту.
- **Полигоны и точки должны быть в одной системе координат.** Полигон в градусах против точек в UTM даёт
  `contains() == False` на всех точках и тихий ноль.
- **Разрывы трека нельзя считать по всем сообщениям подряд:** на стоянке трекер пишет раз в 30 минут, и каждый
  простой попадает в «разрывы». Считать только между сообщениями в движении.
- **Относительный параметр вогнутой оболочки (`ratio` в shapely) не переносится между полями** — он
  масштабируется под размер облака точек. Нужен параметр в метрах.
- **Шаг точек вдоль прохода (35–55 м) больше шага между проходами (5–14 м).** Без сгущения пути альфа-параметр
  хрупкий: сшивает проходы только ценой перекрытия настоящих пропусков.
- Массивные формулы вида `MEDIAN(ABS(range))` в xlsx дают `#VALUE!` при пересчёте — считать в Python и писать
  литералом.
- Округление площадей в ручном учёте непостоянное: заявлена сетка 0,5 га, реально встречается 0,1 га, а случай
  7,57 → 8,0 выходит за обе. Ручные цифры годятся для оценки смещения, но не для проверки отдельного случая —
  нужны сырые.

## 7. Данные, которые надо собирать заранее

- **10–12 новых ручных замеров площади** с сырыми, неокруглёнными числами, из них не менее трети по
  опрыскиванию, с разбросом размеров и хотя бы одним Г-образным контуром. Без них следующий шаг трека не
  стартует. Собирается по мере работ, календарно — недели.
- **Подтверждение сопоставления ~340 единиц техники с `wialon_id`**, включая колонку «объект живой» для дублей
  (6 номеров, у одного три объекта). Автоподбор готовится машиной, подтверждение — только человеком.
- **Ответ владельца:** кто в кластере принял решение по agro-work и что этим людям не нравится в обеих
  программах. Организационный вопрос, задавался, ответа нет. Это самый дешёвый источник для выбора ниши.
- **Проверка, стоят ли iButton-считыватели** на тракторах и есть ли метки. От этого зависит, можно ли вообще
  опознавать навесное оборудование.
- **Решение владельца** по отдельному узкому токену для agro-work и ротации текущего (маска даёт право
  изменять данные, токен один на нас и на них).
- Проверка, имеет ли `10.103.25.14` исход в интернет — без этого пулл по API не поедет.

## 8. Файлы

В репозитории: `docs/GPS_PLAN_FAKT_VISION_ROADMAP.md` (видение и дорожная
карта, 2026-08-12) и `tools/gps_area_method_repro.py` (воспроизведение
замороженного метода с самопроверкой). Остальные файлы трека — вне
репозитория; ниже — где они лежат сейчас и что с ними делать.

| Файл | Где | Что делать |
|---|---|---|
| `HANDOFF_GPS_AGROWORK.md` | выдан в чат | развёрнутая версия истории трека, в project knowledge |
| `wialon_probe.py` … `wialon_probe4.py` | `C:\diag\wialon` | четыре read-only зонда, в project knowledge |
| `wialon_units.csv` | `C:\diag\wialon` | 482 объекта, в project knowledge |
| `wialon_sensors.csv` | `C:\diag\wialon` | 393 строки датчиков, в project knowledge |
| `Проверка_метода_площадей_27_07.xlsx` | выдан в чат | проверочный набор, в project knowledge |
| `Сверка_площадей_GPS_vs_ручной.xlsx` | выдан в чат | калибровочный набор, в project knowledge |
| `wialon_zones.json` | `C:\diag\wialon` | 18,7 МБ, **не прикладывать и не класть в knowledge** |
| `calib_tracks.csv`, `verify_tracks.csv` | `C:\diag\wialon` | рабочие данные, приложить в чат, в knowledge не класть |
| `docs/AGENT_STATE.md`, `docs/TASKS.md`, `docs/RELEASE_GATE.md` | репозиторий | **обновить копии в project knowledge свежими с сервера** |
| `models.py`, `app.py` | репозиторий | то же, копии расходились с репозиторием |
