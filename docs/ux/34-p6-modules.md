# P6 — миграция модулей

Начата 2026-08-09, сразу после закрытия P5 (`33-p5-foundation.md`).

**Задача каждого PR:** привести модуль к контрактам, принятым в P3 и
показанным на эталонах в P4. Не «улучшить дизайн модуля»: ни одно решение
здесь не принимается заново.

Порядок из `00-workflow.md` §14 — от мелких к крупным:
`ref_*`/admin (7) → `wialon` (5) → `work_orders` (4) → `spare_parts` (17) →
`fuel` (21) → добивка. Дроны уже на системе.

---

## 1. UI-P6-001 · `ref_*` / admin — семь экранов

Спецификация задачи — `41-task-specs/UI-P6-001-ref-admin.md`.
Кадры «было / стало» — `52-p6-ref-admin/index.html`.

### 1.1. Счётчики модуля: всё в ноль

| Метрика | Было | Стало | Чем измерено |
|---|---|---|---|
| одноразовых классов таблиц | 7 | **0** | `check_design_system.py --detail`, раздел `table-class` |
| хардкод-цветов | 12 | **0** | тот же вывод |
| строк inline-CSS | 46 | **0** | тот же вывод |
| эмодзи в разметке | 14 | **0** | тот же вывод |
| брейкпоинтов вне шкалы | 0 | 0 | закрыто в P5 |

По проекту: `table-class` 35 → 28, `hardcoded-color` 175 → 161,
`inline-style` 1450 → 1404, `glyph` 106 → 92.

Две из четырнадцати «цветных» строк ушли не из шаблонов, а из
`design-system.css`: вместе с мёртвыми `.vs-badge-role-*` исчезли `#e3f5f2` и
`#0d7d72`, стоявшие в правиле, к которому не обращалась ни одна разметка.

### 1.2. Режим таблицы объявлен числом, а не на глаз

Объёмы — из `10-metrics/ux_metrics.json`, снятых с боевой базы.

| Экран | Строк | Режим | Сортировка по умолчанию |
|---|---|---|---|
| `/ref/customers` | 9 | `is-static` | по названию вверх (совпадает с `ORDER BY` маршрута) |
| `/ref/organizations` | 22 | `is-static` | **не объявлена намеренно** |
| `/admin/users` | 7 | `is-static` | по логину вверх |
| `/admin/permissions` | 7 | `is-static` | по логину вверх |
| `/ref/work_types` | 104 | `is-static` | по названию вверх |
| `/ref/equipment` | **336** | `is-static` | **не объявлена намеренно** |
| `/admin/audit` | 3436, окно 300 | `is-stream` | серверная, заголовки не кнопки |

**Почему у двух экранов умолчания нет.** `data-sort-default` заставляет
`vs-table.js` пересортировать строки **при загрузке**. `/ref/organizations`
приходит в порядке `sort_order`, который владелец расставил руками;
`/ref/equipment` — в трёхуровневом «организация → категория → название».
Клиентское умолчание по одной колонке стёрло бы и то, и другое, и заметить
это можно было бы, только помня прежний порядок. Сортировка по нажатию на
заголовок осталась на обоих.

Это записано тестом `test_client_default_sort_never_overrides_the_server_order`:
он падает, если `data-sort-default` появится на этих двух страницах, и падает,
если исчезнет с четырёх остальных.

**`/ref/equipment` — 336 строк при пороге `is-static` ~200.** Режим объявлен
`is-static`, потому что таблица именно такова: маршрут отдаёт `q.all()` без
предела, разбиения нет. Перевод в `is-paged` — смена того, как оператор ищет
по списку, и это решение владельца. Сам порог в DD-028 помечен непроверенным
предположением с измерением на P7. Пункт открыт, а не закрыт молча.

### 1.3. Журнал аудита: окно, которое не признавалось окном

Маршрут `/admin/audit` всё это время резал выборку: `ORDER BY created_at DESC
LIMIT 300`. Страница писала «Последние действия 300», и число читалось как
итог. На боевой базе записей 3436.

Контракт `is-stream` требует подписи охвата (DD-028). Теперь в подвале стоит
«Показано: 300 / 300» и, когда окно набрано целиком, — «окно последних 300
записей, журнал длиннее, сузьте фильтр».

**Число приходит из маршрута, а не написано в шаблоне.** Константа в двух
местах разошлась бы молча, и подпись стала бы ложной. Проверка —
`test_audit_window_scope_comes_from_the_route`, и её отрицательный контроль
встроен: шаблон рендерится с пределом 7, и в HTML обязано появиться 7. Если бы
шаблон печатал 300 своей рукой, подмена ничего не изменила бы — то есть
проверка не отличала бы верный код от неверного.

Правка `app.py` — две строки, объявлена в описании PR.

### 1.4. Статус: один компонент вместо цвета в атрибуте

Роль пользователя красилась так — в двух шаблонах, в теле строки таблицы:

```
style="...{% if u.role == 'admin' %}background:#e74c3c;color:#fff;
       {% elif u.role == 'operator' %}background:#27ae60;color:#fff;
       {% else %}background:#2980b9;color:#fff;{% endif %}"
```

Рядом в дизайн-системе лежали `.vs-badge-role-admin`, `-operator`,
`-branch_operator`, `-viewer` и три `.vs-badge-cat-*` — **семь правил, к
которым не обращалась ни одна разметка**. Класс существовал, компонента не
было.

DD-032 говорит прямо: классы под значение справочника удаляются, остаётся один
компонент, которому передаётся семантическая роль. Семь правил удалены,
соответствие «роль → тон» объявлено в шаблоне, где оно и есть решение о
показе.

### 1.5. Что нашла проверка доступности — и почему её пришлось расширить

axe по семи экранам, на десктопе **и** на 390 px, 16 прогонов:
**0 serious, 0 critical, 0 moderate, 0 minor.**

Ноль получился не сразу. Первый прогон дал **4 serious**,
`scrollable-region-focusable`, на `/ref/customers` и `/ref/work_types`, и на
обоих вьюпортах. Цель — `<ul>`, не таблица.

Причина в моей собственной правке. Из четырёх шаблонов список диагностики был
одинаков в трёх, а в `ref_customers` нёс `max-height: 220px; overflow: auto`.
Унифицируя, я взял «самый полный» вариант — и вместе с ним разнёс на три
страницы прокручиваемую область без `tabindex`, недостижимую с клавиатуры.
На `/ref/customers` этот дефект существовал и до трека.

`max-height` и `overflow` сняты: список короткий, ради него не нужен скролл.
После правки — ноль на всех шестнадцати прогонах.

**Это и есть отрицательный контроль проверки доступности, поставленный самой
работой:** с `max-height` axe даёт 4 serious, без него — 0, на тех же
страницах и тех же данных. Проверка различает исправный случай и неисправный.

### 1.6. Мёртвый код, обещавший другое устройство страницы

В `ref_work_types.html` лежали **два** объявления `editWt`. Первое обращалось
к элементам `wt-edit-<id>`, которых в разметке нет вовсе, — на нём страница
падала бы при первом нажатии «Изменить». Работала она потому, что второе
объявление, ниже по файлу, перекрывало первое.

То есть читатель файла видел устройство страницы, которого не существует.
Первый блок удалён вместе с `cancelWtEditLegacy` (не вызывается ниоткуда).
Тест `test_work_types_defines_edit_handler_exactly_once` держит это состояние;
отрицательный контроль — возврат блока роняет ровно его.

### 1.7. Отпечаток фикстур: проверка, сломавшаяся о собственную правку

Оба съёмочных стенда перед первым кадром сверяют, что экземпляры «до» и
«после» держат **одни и те же данные**: сравниваются тексты ячеек таблицы на
одной странице. Страница была константой — `/ref/equipment`.

В этом PR `/ref/equipment` и правится. Пустая ячейка «единица измерения»
стала рисовать тире, число непустых ячеек выросло с 183 до 187, и сверка
сообщила о расхождении **данных**, которого нет.

Страница отпечатка стала параметром `--fingerprint` обоих скриптов. Годная —
`/drones/works`: 220 ячеек, совпали побайтово. По дороге выяснилось, что
`/fuel/transactions`, `/wialon` и `/spare-parts` для этой роли **не годятся
вовсе**: их ячейки содержат время посева, и два экземпляра, поднятые с
разницей в минуту, честно показывают разные значения. Это записано, потому что
следующий модуль наступит на то же место.

### 1.8. Тесты

`tests/test_ref_admin_module.py`, 14 тестов. Всего в проекте 518 → **532**,
ни один не удалён и не ослаблен.

Отрицательный контроль прогнан по пяти проверкам — на каждой код ломался
намеренно, и падала ровно она:

| Внесённая поломка | Кто поймал |
|---|---|
| у `/ref/organizations` появилось клиентское умолчание сортировки | `test_client_default_sort_never_overrides_the_server_order` |
| у таблицы аудита убран режим | `test_every_table_declares_a_mode` |
| у одной иконочной кнопки убран `aria-label` | `test_row_actions_all_carry_an_accessible_name` |
| вернулся мёртвый блок `editWt` | `test_work_types_defines_edit_handler_exactly_once` |
| шаблон печатает предел аудита своей рукой | `test_audit_window_scope_comes_from_the_route` |

### 1.9. Контроль: страницы вне модуля не изменились

`tools/ux/shoot_regression.mjs`, база — коммит `92d0810` (закрытие P5),
72 маршрута × 4 вьюпорта × 2 экземпляра.

| Что | Строк | Медиана | Максимум |
|---|---|---|---|
| **вне модуля** (контроль) | 264 | **0,00 %** | **0,11 %** |
| внутри модуля (менялось намеренно) | 24 | — | 26,58 % |

Максимум контроля — 0,11 % на `/fuel/api/fuel_ping`, странице со временем
ответа; следующие за ней 0,05 % и 0,04 %. Ни одна страница за пределами семи
экранов модуля не сдвинулась.

Внутри модуля изменились ровно те шесть страниц, которые правились
(`/admin/audit` в набор обхода не входит — он исключён как зависящий от числа
входов краулера):

| Экран | Доля пикселей |
|---|---|
| `/ref/work_types` | 14,26 … 26,58 % |
| `/admin/users` | 15,57 … 24,60 % |
| `/ref/organizations` | 17,79 … 24,51 % |
| `/ref/equipment` | 18,36 … 23,51 % |
| `/ref/customers` | 19,29 … 22,65 % |
| `/admin/permissions` | 12,52 … 17,69 % |

**Новых горизонтальных переполнений — 0. Снято — 11.** Это измеренный
результат, а не обещание: до миграции таблицы модуля вылезали за край окна на
`tablet`, `narrow` и `mobile`, теперь они прокручиваются внутри собственной
области `.vs-table-scroll`.

| Снято переполнение | Вьюпорты |
|---|---|
| `/ref/equipment` | tablet, narrow, mobile |
| `/admin/users` | tablet, narrow, mobile |
| `/ref/customers` | narrow, mobile |
| `/ref/organizations` | narrow, mobile |
| `/ref/work_types` | mobile |

Из 23 переполнений, известных с P5, модуль закрыл 11.

### 1.10. Поведение, которое тесты проекта проверить не могут

Общие строки редактирования переведены с `style="display:none"` на атрибут
`hidden`, а показ и скрытие — с `el.style.display` на `el.hidden`. В тестах
проекта **нет движка JS**: они рендерят HTML и сравнивают строки. Шаблон
рендерится одинаково верно и при работающем коде, и при сломанном, поэтому
строковый тест здесь ничего не различает.

Второе: клиентская сортировка перекладывает **все** строки `tbody`, включая
скрытые служебные. Нужно доказательство, что после сортировки они остались
скрытыми, а открытая форма по-прежнему встаёт под свою строку.

`tools/ux/check_ref_admin_interactions.mjs` — 17 проверок в настоящем
браузере на поднятом экземпляре, все зелёные:

- строка редактирования скрыта при загрузке, появляется по нажатию, встаёт
  ровно под изменяемой строкой, заполняется из `data-*`, прячется по «Отмена»;
- после клиентской сортировки скрытые служебные строки остались скрытыми;
- у техники изменяемая строка скрывается, по «Отмена» возвращается;
- поле «новая модель» показывается и прячется атрибутом `hidden`;
- селекты строки редактирования наполняются из JS (23 варианта организаций);
- мультиселект инициализируется («Категория: Все (9)»);
- у пользователей нажатие «Изменить» заполняет форму сверху.

В CI не входит: требует поднятого экземпляра. Запуск — в спецификации задачи.

### 1.11. Пункты, оставленные владельцу

Перечислены в `docs/tracks/ui.md`, раздел «Открытые пункты». Коротко:
`audit_logs.html` целиком по-русски и узбекских формулировок в словаре нет;
три ключа `t()` отсутствуют в словаре и печатаются как есть; `/ref/equipment`
выше порога `is-static`; `t('Фаол')` переведено множественным числом.

---

## 2. UI-P6-002 · `wialon` — пять экранов

Спецификация задачи — `41-task-specs/UI-P6-002-wialon.md`.
Кадры «было / стало» — `53-p6-wialon/index.html`.

### 2.1. Счётчики модуля: всё в ноль

| Метрика | Было | Стало |
|---|---|---|
| одноразовых классов таблиц | 7 | **0** |
| хардкод-цветов | 9 | **0** |
| строк inline-CSS | 40 | **0** |
| эмодзи в разметке | 36 | **0** |

По проекту: `table-class` 28 → 21, `hardcoded-color` 161 → 154,
`inline-style` 1404 → 1364, `glyph` 92 → 56.

### 2.2. Три экрана, где сортировки не будет никогда

Контракт DD-029 требует сортируемых заголовков в режиме `is-static`. На трёх
экранах Виалона их не будет, и причина — не оформление.

Маршрут разбирает эти формы **позиционно**:

```
getlist('vialon_name')[i]  ↔  getlist('eq_choice')[i]
```

Порядок значений в форме — это порядок строк в разметке. Клиентская
сортировка переставила бы строки, а вместе с ними и пары «объект → техника»,
и моточасы записались бы **не той машине**. Ни одной ошибки на экране при
этом не появилось бы.

У отчёта причина своя: колонка «№» нумерует строки в порядке маршрута, и
сортировка оставила бы номера на местах — они начали бы врать.

Сортировка включена ровно на одном экране модуля — списке связок. Там
позиционной связки нет: у каждой строки своя форма по `id`.

**Проверка этого правила сама оказалась пустой, и это стоит знать.** Первая
редакция теста смотрела отрисованную страницу — и проходила на сломанном коде
по двум причинам сразу: таблицы `wialon_auto_match` лежат внутри `{% macro %}`
и без предложений для разбора не рендерятся вовсе (цикл делал ноль итераций),
а `wialon_mapping.html` не рендерится ни при каких условиях (§2.5). Поймал это
отрицательный контроль: `data-sortable` дописали, тест остался зелёным.
Проверка переведена на исходник и требует, чтобы таблицы были найдены.

### 2.3. `hidden` не работал, и разметка об этом не говорила

Показ и скрытие полей периода переведены с `el.style.display` на атрибут
`hidden`. В HTML атрибут стоял, тесты по строке были честны — а на экране поля
«Ихтиёрий» рисовались во всех режимах.

Причина в каскаде. Браузер объявляет `[hidden]{display:none}`, но на **уровне
UA**, и любое авторское правило с `display` его перебивает.
`.vs-field{display:block}` — ровно такое. Специфичность при этом **равна**
(один класс против одного атрибута), то есть исход решает порядок правил в
файле — и завтра он поменяется от того, куда кто допишет строку.

Добавлено `[hidden] { display: none !important; }`.

**У этой правки есть последствие за пределами модуля, и его надо назвать.**
Та же комбинация — `<div class="vs-field" ... hidden>` — стоит в
`spare_parts_catalog.html` (`#catActiveField`) и `spare_parts_skus.html`
(`#skuActiveField`). Значит, поле, которое код прятал, **всё это время
показывалось**. Правка каскада чинит и эти два экрана: они изменятся в
регрессионном прогоне, хотя в мигрируемый модуль не входят. Это исправление
дефекта, а не побочный ущерб, но выглядит оно в цифрах как изменение вне
модуля, поэтому названо здесь поимённо.

**Строковый тест такого не ловит и не мог поймать.** Отличить «атрибут стоит»
от «элемент не виден» умеет только рендер. Поэтому проверка живёт в
`tools/ux/check_wialon_interactions.mjs` — 20 проверок в настоящем браузере, —
а нашёл дефект скриншот из съёмки «было / стало».

Тот же дефект был и до миграции, но в другом виде: в `wialon_report.html`
инлайновый стиль объявлял `display:none` и **следом** `display:flex` в том же
атрибуте. Второе побеждало последним, и поля периода стояли на экране всегда.

### 2.4. Скрытые поля лежали вне ячеек

В `wialon_mapping.html` пять `<input type="hidden">` на строку стояли прямо в
`<tbody>`, между `</tr>` и `<tr>`. Парсер выносит такие узлы за пределы
таблицы (foster parenting) — они оказывались **перед** `<table>`. Форма
продолжала отправляться правильно, потому что относительный порядок узлов при
выносе сохраняется, но разметка была невалидной, а связь значения со строкой
держалась на порядке, которого в разметке уже не видно.

Поля перенесены внутрь первой ячейки строки.

### 2.5. Экран, которого нет

`wialon_mapping.html` **недостижим**:

1. `render_template('wialon_mapping.html', ...)` не вызывается ниоткуда —
   полный поиск по репозиторию даёт ноль;
2. форма отправляется на `url_for('wialon_save')`, а эндпойнта с таким именем
   в приложении нет вовсе. Попытка отрисовать шаблон падает `BuildError` ещё
   до первой строки разметки.

То есть экран ручного разбора импорта перестал существовать раньше, чем этот
файл. Шаблон приведён к контрактам вместе с остальным модулем, но **удалять
его или восстанавливать тракт — решение владельца**. Пока это не решено, шапка
файла говорит о его состоянии прямо, а тест
`test_dead_mapping_screen_is_named_dead` держит эту пометку на месте и падает,
если шаблон снова начнут рендерить, не сняв её.

### 2.6. Латиница в узбекских подписях

`wialon_mapping.html` нёс 26 подписей вида

```jinja
{% set L_title = 'Виалон — Маппинг' if is_ru else 'Wialon — Mapping' %}
```

Обе ветки — **один язык**, узбекский, в двух алфавитах. Устав допускает
латиницу только в названиях продуктов и брендов. Латинская ветка убрана: это
переписывание того, что уже лежало в файле, а не перевод.

Следствие названо прямо, а не замазано: **русской стороны у этих подписей
нет**, русский пользователь видит узбекский. Русские формулировки — слово
владельца; пункт открыт в `docs/tracks/ui.md`.

### 2.7. Два заголовка на одном экране

Все пять шаблонов рисовали свой `<h1>` в содержимом, хотя `base_next` уже
рисует заголовок экрана из `{% block title %}` — с другим текстом. Свои `<h1>`
убраны, кнопки действий оставлены полосой над содержимым, как в модуле
справочников.

### 2.8. Тесты и проверки

`tests/test_wialon_module.py`, 15 тестов. Всего в проекте 532 → **547**.

Отрицательный контроль прогнан по шести проверкам; на каждой код ломался
намеренно, и падала ровно ожидаемая:

| Внесённая поломка | Кто поймал |
|---|---|
| таблица решения сделана сортируемой | `test_positional_decision_forms_are_never_sortable` |
| скрытое поле вынесено из ячейки | `test_hidden_inputs_live_inside_cells` |
| поле периода лишилось `hidden` | `test_report_period_fields_are_hidden_outside_range_mode` |
| вернулась латиница в подписи | `test_no_latin_uzbek_left_in_the_mapping_screen` |
| `colspan` вернулся к 6 при пяти колонках | `test_shared_rows_span_the_real_number_of_columns` |
| снята пометка «шаблон недостижим» | `test_dead_mapping_screen_is_named_dead` |

`tools/ux/check_wialon_interactions.mjs` — 20 проверок в настоящем браузере:
поля периода скрыты и появляются по переключению режима, общая строка
редактирования встаёт под своей строкой, служебные строки остаются скрытыми
после клиентской сортировки, фильтр по статусу сужает список (9 из 22),
селекты наполняются из JS (23 варианта), липкая панель на месте.

axe по пяти экранам, десктоп и 390 px, 10 прогонов: **0 serious, 0 critical,
0 moderate, 0 minor**.

### 2.9. Контроль: что двинулось за пределами модуля

`tools/ux/shoot_regression.mjs`, база — `9bea874`, 72 маршрута × 4 вьюпорта.

Прогон не успел закончиться к моменту коммита `63c812d`, и в его описании это
сказано прямо вместе с ожиданием, которое следовало проверить. Ожидание
подтвердилось полностью.

| Что | Строк | Медиана | Максимум |
|---|---|---|---|
| **вне модуля, кроме двух экранов запчастей** | 264 | **0,00 %** | **0,11 %** |
| два экрана запчастей — следствие правки `[hidden]` | 8 | — | 14,79 % |
| внутри модуля (менялось намеренно) | 16 | — | 53,26 % |

Максимум контроля — 0,11 % на `/fuel/api/fuel_ping`, странице со временем
ответа. **Ни одна другая страница за пределами модуля не сдвинулась ни на
сотую долю процента.**

Два экрана запчастей сдвинулись ровно так, как и должно было выйти из правки
каскада, — и **стали короче**, потому что поле, которое код прятал, перестало
рисоваться:

| Экран | Вьюпорт | Доля | Высота |
|---|---|---|---|
| `/spare-parts/skus` | mobile | 14,79 % | 2885 → 2819 |
| `/spare-parts/skus` | laptop | 12,35 % | 2173 → 2104 |
| `/spare-parts/catalog` | laptop | 6,02 % | 4000 → 3932 |
| `/spare-parts/catalog` | mobile | 5,32 % | 5594 → 5527 |

Разница высоты ≈ 68 px — это ровно одна строка поля формы. На `tablet` и
`narrow` высота не изменилась (доля 0,19–0,37 %): там поле стояло в
переносящемся ряду, и его исчезновение общей высоты не меняет.

**Новых горизонтальных переполнений — 0. Снято — 3**
(`/wialon/auto_match` на mobile, `/wialon` на mobile и narrow).

---

## 3. UI-P6-003 · `work_orders` — четыре экрана

Кадры «было / стало» — `54-p6-work-orders/index.html`.

### 3.1. Счётчики модуля

| Метрика | Было | Стало |
|---|---|---|
| одноразовых классов таблиц | 1 | **0** |
| хардкод-цветов | 1 | **0** |
| строк inline-CSS | 76 | **0** |
| эмодзи в разметке | 1 | **0** |

По проекту: `table-class` 21 → 20, `hardcoded-color` 154 → 153,
`inline-style` 1364 → 1288, `glyph` 56 → 55.

### 3.2. Цвет статуса приходил из Python

`WO_STATUS_COLORS` сопоставлял статусу переменную слоя совместимости —
`var(--text2)`, `var(--info)`, `var(--warn)` — а шаблоны рисовали её
инлайновым `style="color:{{ status_colors[wo.status] }}"`. То есть цвет решал
маршрут, а как его приложить — каждый шаблон отдельно. Ровно это DD-032 и
убирает.

Теперь словарь несёт **семантику**: `WO_STATUS_TONES` отдаёт имя модификатора
`.vs-badge-*` — один источник на пять статусов и два шаблона, палитра остаётся
в одном месте. `test_status_tones_cover_every_status` падает, если набор тонов
разойдётся с набором подписей.

### 3.3. Латиница: около семидесяти подписей

В отличие от Виалона, русская сторона здесь настоящая русская, а узбекская
была просто записана не тем алфавитом. Это переписывание алфавита без
изменения смысла: `Buyurtmalar` → `Буюртмалар`, `Muddati o‘tgan` →
`Муддати ўтган`, `Rejalashtirilgan hajm` → `Режалаштирилган ҳажм`.

`test_no_latin_uzbek_left_in_the_module` обходит каждую ветку `else '…'` и
падает на одной латинской букве.

### 3.4. Два вида одного списка

Ниже 768 px список показывается карточками, выше — таблицей; разметка
отдаётся целиком, переключает CSS. Было `.wo-cards` / `.wo-table-wrap` в
инлайновом `<style>`, стало `.vs-list-cards` / `.vs-table-wrap.is-wide-only` в
дизайн-системе, на границе DD-024 — той же, на которой сайдбар уходит в
overlay. Одно число на два решения, а не два близких.

`test_table_and_cards_show_the_same_orders` сверяет номера нарядов в обоих
видах: разойдись они — оператор с телефона увидел бы не тот список, и ничто на
экране об этом не сказало бы.

### 3.5. Что нашла проверка доступности

Первый прогон дал **20 critical** на `/work-orders/new`: `label` (6 узлов) и
`select-name` (4 узла), на десктопе и на 390 px.

Причина — не миграция: подписи в этой форме никогда не были связаны со своими
полями. Было `<label>Дата *</label>` и следом `<input class="form-control">` —
ни `for`, ни вложенности. Для человека с экранным диктором форма из
одиннадцати полей звучала как одиннадцать безымянных полей ввода.

Проставлены `for`/`id` — 11 пар в форме создания и 4 в форме закрытия.
После правки: **0 serious, 0 critical, 0 moderate, 0 minor** на всех шести
прогонах.

Это второй случай подряд, когда контракт доступности вскрыл дефект, который
вёрстка не создавала, а унаследовала. Первый — `scrollable-region-focusable`
на списке диагностики справочников (§1.5).

### 3.6. Кадры сняты на ПУСТОМ списке, и это надо знать

Съёмка «было / стало» прошла без сбоев — 20 кадров, 0 ошибок, — но на экране
`/work-orders` **нет ни одного наряда**: полоса показателей показывает 0/0/0/0,
а тело списка — пустое состояние. То есть ни таблица, ни карточный вид на
кадрах не видны вовсе.

Причина в фикстурах, а не в вёрстке. `work_orders` входит в цикл внешних
ключей (`daily_records ↔ spare_part_request_items ↔ spare_part_skus ↔
spare_parts ↔ work_orders`), и `db.metadata.sorted_tables` исключает такие
таблицы из порядка; посев по ним не проходит. В журнале эфемерного экземпляра
это видно строкой SAWarning и итогом «seeded 65 tables» — без нарядов.

**Что из этого следует для критерия 4.** Дифф «до/после» по этому модулю
показывает шапку, фильтры и пустое состояние — и по ним он честен. Таблицу и
карточный вид он не покрывает; их поведение закрыто тестами на настоящих
данных (`test_table_and_cards_show_the_same_orders`,
`test_list_table_declares_a_mode_and_carries_no_one_off_class`), причём с
отрицательным контролем. Утверждать, что список просмотрен глазами, было бы
неправдой, поэтому это сказано здесь.

Пункт для следующего прогона: посеять `work_orders` отдельным шагом, как это
уже сделано для единиц измерения и статусов замкнутых наборов
(`seed_daily_units`, `seed_closed_set_statuses`). Работа на два десятка строк,
но она меняет фикстуры сразу на обоих экземплярах и потому делается отдельно
от миграции модуля.

### 3.7. Контроль: за пределами модуля не двинулось ничего

`tools/ux/shoot_regression.mjs`, база — `6610ce2`, 72 маршрута × 4 вьюпорта.

| Что | Строк | Медиана | Максимум |
|---|---|---|---|
| **вне модуля** | 280 | **0,00 %** | **0,13 %** |
| внутри модуля (менялось намеренно) | 8 | — | 31,08 % |

Весь «шум» контроля — две страницы, печатающие время: `/fuel/api/fuel_ping`
(0,13 / 0,07 / 0,06 / 0,03 %) и `/fuel/report` (0,04 / 0,03 %). Ни одна другая
из 280 строк не отличается ни на сотую долю процента.

В отличие от UI-P6-002, следствий за пределами модуля здесь нет вовсе:
правки этого PR не трогали ни каскад, ни общие правила раскладки — только
разметку четырёх шаблонов и один словарь в `work_orders.py`.

| Экран | Вьюпорт | Доля | Высота |
|---|---|---|---|
| `/work-orders` | mobile | 31,08 % | 883 → 961 |
| `/work-orders` | narrow | 30,05 % | 900 → 900 |
| `/work-orders` | tablet | 24,62 % | 768 → 768 |
| `/work-orders` | laptop | 23,58 % | 900 → 900 |
| `/work-orders/new` | tablet | 13,36 % | 768 → 768 |
| `/work-orders/new` | mobile | 13,33 % | 1184 → 1151 |
| `/work-orders/new` | narrow | 12,86 % | 1167 → 1104 |
| `/work-orders/new` | laptop | 9,58 % | 900 → 900 |

Доля по `/work-orders` велика при пустом списке потому, что менялась именно
верхняя часть экрана — заголовок, полоса показателей, панель фильтров, — а
она и составляет почти весь видимый кадр (§3.6).

**Новых горизонтальных переполнений — 0.** Снятых тоже 0: у этого модуля их и
не было.

**Итог по критериям выхода UI-P6-003:** выполнены все шесть. Критерий 4 — с
оговоркой §3.6: список нарядов на кадрах пуст, и его два вида закрыты
тестами, а не глазами.

---

## 4. UI-P6-004 · `spare_parts` — шестнадцать экранов

Модуль пришёл в куда лучшем состоянии, чем предыдущие три: **ноль
хардкод-цветов, ноль одноразовых классов таблиц**, все 24 таблицы уже на
`vs-table`. Разрыв был в другом.

### 4.1. Счётчики модуля

| Метрика | Было | Стало |
|---|---|---|
| строк inline-CSS | 46 | **0** |
| эмодзи в разметке | 32 | **0** |
| таблиц без объявленного режима | **24** | **0** |
| хардкод-цветов | 0 | 0 |
| одноразовых классов таблиц | 0 | 0 |

По проекту: `inline-style` 1288 → 1242, `glyph` 55 → 23, `table-class` 20,
`hardcoded-color` 153.

### 4.2. Двадцать четыре таблицы молчали о своём режиме

`vs-table` без `is-static`/`is-paged`/`is-stream` **проходит чекер по
классам** — и ничего не говорит о том, откуда берётся сортировка и что
считает итог. Ровно поэтому DD-028 требует объявлять режим явно, и ровно
поэтому одного чекера мало.

Объёмы модуля с боевой базы: `spare_parts` 250, `equipment_models` 55,
`spare_part_inventory` 16, `spare_part_skus` 16, остальные — единицы. Все
таблицы получили `is-static`.

**Умолчание сортировки не объявлено ни на одной.** Заголовки — кнопки, но
порядок при загрузке остаётся тем, который отдал маршрут. Это то же правило,
что в трёх предыдущих модулях, и здесь оно применено ко всем сразу, потому
что выяснять `ORDER BY` двадцати четырёх выборок ради умолчания — работа, не
дающая ничего, кроме риска подменить осмысленный порядок.

**Кроме одной таблицы, где сортировки не будет никогда:** позиции заявки в
`spare_part_form.html`. Маршрут разбирает форму позиционно —
`getlist('item_name')[i]` к `getlist('item_quantity')[i]`,
`getlist('item_sku_id')[i]`. Клиентская сортировка отдала бы количество одной
позиции другой, молча.

### 4.3. Цвет статуса приходил из Python — второй раз за фазу

`STATUS_COLORS` и `PRICE_STATUS_COLORS` отдавали `var(--text2)`,
`var(--accent)`, `var(--warn)`, а шаблон красил инлайновым `style="color:"`.
Ровно то же было в нарядах (§3.2). Оба словаря переведены на **тон**:
`STATUS_TONES`, `PRICE_STATUS_TONES` — имя модификатора `.vs-badge-*`.

Одно решение при переносе сохранено дословно: SP-CANCEL-008 намеренно даёт
отмене **тот же** тон, что и отказу — это два исхода «не состоялось», а
различает их подпись. Тест это фиксирует: `cancelled == rejected` и
`cancelled != draft`.

Точка ленты событий в карточке заявки брала цвет из того же словаря отдельным
набором `var(--vs-*)`. Теперь она берёт тон из общего словаря.

### 4.4. Обход дизайн-системы, который сам себя объяснил

В `spare_parts_desk.html` лежало правило с таким комментарием:

> `design-system.css` объявляет `.vs-kpi` ПОСЛЕ `.vs-card-accent-*`, поэтому
> сокращение `border:` у плитки молча сбрасывает `border-left` акцента при
> равной специфичности. Восстанавливаем подсказку здесь (в области страницы,
> выше по специфичности), а не правкой `design-system.css`, которая изменила
> бы главный дашборд — вне объёма SP-DESK-001.

Автор нашёл дефект, описал его точно и обошёл, потому что общий файл трогать
было нельзя. Теперь можно: правила `.vs-card-accent-*` перенесены **после**
`.vs-kpi`, и сокращение больше не стирает акцент ни у чего.

Сочетание `vs-kpi` + `vs-card-accent-*` во всей разметке продукта **одно** —
та самая плитка «готово к выдаче», — поэтому перенос ничего больше не
затрагивает. Обход удалён.

### 4.5. Проверка, которой не хватало: рендер модуля

Чекеры читают **исходники**. Этого не хватает по трём причинам, и все три
нашлись здесь:

1. шаблон может быть чист, а страница падать — переименование ключа контекста
   (`status_colors` → `status_tones`) даёт HTTP 500, которого в исходнике не
   видно вовсе;
2. глиф может приходить из данных или из включённого партиала;
3. таблица может лежать в партиале, и «все таблицы модуля объявили режим» по
   файловой маске окажется неправдой.

Заведён `tools/ux/check_module_render.mjs`: открывает каждый экран настоящим
браузером и требует HTTP 200 без редиректа в `/login`, ноль глифов в видимом
тексте, ноль `.vs-table` без режима, ноль ошибок JS. По четырнадцати экранам
модуля — **чисто**.

**Его первый прогон дал 12 падений из 14, и это был ложный сигнал**, о котором
стоит знать: перезапуск эфемерного экземпляра молча не состоялся («Address
already in use»), и проверка сняла показания со **старого процесса** —
питоновский код прежний, шаблоны новые. Отсюда и 500, и глифы. После честного
перезапуска — ноль по всем четырём измерениям. Вывод для следующего модуля:
после перезапуска стенда убеждаться, что слушает именно новый процесс.

### 4.6. Чекер поймал мою собственную правку

Первая редакция навесила на таблицу позиций класс `.sp-form-table` ради
выравнивания ячеек по верху. `check_design_system.py` немедленно показал
`table-class 20 → 21` — и был прав: контракт разрешает у `.vs-table` только
модификаторы `is-*`, а именно так одноразовые классы таблиц и заводились
раньше. Заменено на `.vs-table.is-top`.

### 4.7. Кадры и доступность

Съёмка «было / стало» — `55-p6-spare-parts/index.html`, 40 кадров, 0 сбоев.
Пять экранов: рабочий стол, карточка заявки, каталог, новая заявка, остатки.

axe по пяти экранам, десктоп и 390 px, 10 прогонов: **0 serious, 0 critical,
0 moderate, 0 minor** — с первого раза, в отличие от двух предыдущих модулей.

На кадре рабочего стола видно, что зелёная полоска у плитки «готово к выдаче»
рисуется **из дизайн-системы**, а обход со страницы удалён (§4.4). На кадре
новой заявки видно, что скрепка и крестик — иконки набора, отрисованные
скриптом из констант (§4.1).

Регрессионный прогон на момент коммита ещё считался; его числа идут отдельно,
как это было с UI-P6-002 и UI-P6-003.

### 4.8. Контроль — и загрязнённое измерение, которое я устроил сам

`tools/ux/shoot_regression.mjs`, база — `7e2abab`, 72 маршрута × 4 вьюпорта.

| Что | Строк | Медиана | Максимум |
|---|---|---|---|
| вне модуля | 216 | 0,00 % | **3,15 %** |
| внутри модуля (менялось намеренно) | 72 | — | 13,41 % |

Максимум контроля — **3,15 % на `/drones/customers`, вьюпорт mobile**, при
нулях на остальных трёх и высоте 4404 → 4393. Это выше порога, объявленного в
P5 (≤ 1 % вне изменённой полосы), и оставлять это числом без объяснения
нельзя.

**Причина найдена, и она в моей работе, а не в модуле.** Пока прогон ещё
снимал кадры, я начал следующий модуль и добавил в `design-system.css`
правило:

```css
.vs-table summary { color: var(--vs-text-muted); font-size: var(--vs-fs-sm); }
```

Стенд отдаёт CSS файлом с диска на каждый запрос, а не снимком на старте.
Значит страницы, снятые **после** этой правки, рендерились с другим
стилем — и `/drones/customers`, где внутри `.vs-table` есть два
`<details>/<summary>`, поменял высоту на 11 px.

То есть числа этого прогона сняты на двух разных версиях стилей, и доверять
им нельзя — ни максимуму контроля, ни «новых переполнений 0».

**Правило, которого не хватало:** пока идёт прогон, общий файл стилей не
трогать. Стенд не делает снимка стилей на старте, и любая правка попадает в
середину измерения, не оставив следа в отчёте.

Прогон по запчастям **должен быть переснят начисто**. Это записано как первый
шаг работ по модулю АЗС: его собственный прогон пойдёт от базы, в которой
правило `summary` уже есть с обеих сторон, и заодно покажет, было ли на
`/drones/customers` что-то кроме этих 11 px.

---

## 5. UI-P6-005 · `fuel` — начато: семейство отчётов

Модуль АЗС — последний и самый крупный: 21 шаблон, 4525 строк. На момент
начала он держал **1022 из 1242** строк inline-CSS всего проекта и **19 из 20**
одноразовых классов таблиц. То есть оставшийся долг — это он.

Делается частями. Первая — четыре экрана отчётов.

### 5.1. Один и тот же лист печати, скопированный четыре раза

`report_card_issues`, `report_counter_mismatch`, `report_station_totals` несли
**побайтово одинаковый** блок `@media print` (md5 `24778f9f9f91`, 572 байта),
`balance_report` — тот же блок с добавленными своими элементами.

Правило печати — поведение продукта, а не одного экрана. Пока оно лежало в
шаблонах, каждый новый отчёт начинался с копирования блока, а любая правка
требовала четырёх одинаковых — то есть рано или поздно они разошлись бы.

Блок перенесён в `design-system.css` один раз. Скрываемое при печати
объявляется классом `.vs-no-print`: экран помечает свои элементы и не трогает
общее правило. Цвета печати вынесены в отдельные токены
(`--vs-print-bg`, `--vs-print-border`, `--vs-print-muted`) — на бумаге нет ни
темы, ни фона приложения, и смешивать их с экранными нельзя.

### 5.2. Счётчики первой части

| Экран | inline-CSS | цвета | классы таблиц | эмодзи |
|---|---|---|---|---|
| `report_card_issues` | 16 → **0** | 6 → **0** | 1 → **0** | 0 |
| `report_counter_mismatch` | 19 → **0** | 6 → **0** | 1 → **0** | 0 |
| `report_station_totals` | 15 → **0** | 4 → **0** | 1 → **0** | 0 |
| `station_issues_report` | 19 → **0** | 3 → **0** | 1 → **0** | 3 → **0** |

По проекту: `inline-style` 1242 → 1173, `hardcoded-color` 153 → 134,
`table-class` 20 → 16, `glyph` 23 → 20.

Три одинаковых хардкода `#fefce8` на итоговой строке и два `#f0f9f0`/`#166534`
на строке-группе стали модификаторами `.vs-table tbody tr.is-total` и
`tr.is-group` — на токенах, прошедших расчёт контраста DD-021.

### 5.3. Что осталось в модуле АЗС

| Экран | inline-CSS | цвета | классы таблиц |
|---|---|---|---|
| `receipts` | 172 | 3 | 1 |
| `warehouses` | 171 | 0 | 1 |
| `stations` | 161 | 1 | 1 |
| `report` | 143 | 0 | 3 |
| `warnings` | 142 | 0 | 0 |
| `dashboard` | 97 | 4 | 1 |
| `balance_report` | 30 | 3 | 2 |
| остальные восемь | 37 | 12 | 5 |

Первым шагом следующей части — **чистый перепрогон регрессии по запчастям**
(§4.8).

### 5.4. Вторая часть: три тяжёлых экрана и шесть компонентов под тремя именами

`receipts`, `warehouses`, `stations` — 504 строки собственного CSS на троих.
Читать там было почти нечего: **один и тот же набор компонентов, заведённый
трижды под именами задач**.

| Что это на самом деле | receipts | warehouses | stations |
|---|---|---|---|
| полоса показателей | `.fuel002a-context-strip` + `-pill` | `.fuel002c-…` | `.fuel002e-…` |
| карточки-подсказки | `.fuel002a-guidance-panel` + `-card` | `.fuel002c-…` | `.fuel002e-…` |
| шапка экрана с `<h1>` | `.fuel002a-page-header` | `.fuel002c-…` | `.fuel002e-…` |
| обёртка таблицы | `.fuel002a-table-wrap` | `.fuel002c-…` | `.fuel002e-…` |
| сама таблица | `.fuel002a-table` | `.fuel002c-warehouse-table` | `.fuel002e-stations-table` |
| карточка формы | `.fuel002a-form-card` | `.fuel002c-form-card` | `.fuel002e-form-card` |

Это ровно тот симптом, ради которого трек заведён (§2 трек-файла): инкремент
делает свой CSS вместо общего компонента, потому что ничто этого не
запрещает — и делает его трижды.

Сопоставление оказалось буквальным, без подгонки:
`-context-strip` — сетка из четырёх ячеек с подписью и значением, то есть
`vs-kpi-strip`; `-guidance-card` — карточка с `border-left: 4px solid
var(--accent)`, то есть `vs-card vs-card-accent-success`; `-table-wrap` —
`overflow-x: auto`, то есть `vs-table-scroll`; `-field-missing` — красная
рамка поля, то есть `vs-input.is-error`. Новым оказался ровно один компонент —
сетка подсказок `vs-guide-grid`.

| Экран | inline-CSS | цвета | классы таблиц | эмодзи |
|---|---|---|---|---|
| `receipts` | 172 → **0** | 3 → **0** | 1 → **0** | 1 → **0** |
| `warehouses` | 171 → **0** | 0 | 1 → **0** | 1 → **0** |
| `stations` | 161 → **0** | 1 → **0** | 1 → **0** | 2 → **0** |

По проекту: `inline-style` 1173 → **669**, `hardcoded-color` 134 → **130**,
`table-class` 16 → **13**, `glyph` 20 → **16**.

### 5.5. Защита от двойного нажатия, которой не существовало

В `warehouses.html` скрипт вешает на форму удаления класс
`fuel002c-delete-submitted` при отправке. **Правила у этого класса не было
никогда** — ни в инлайновом `<style>` шаблона, ни в дизайн-системе; проверено
по истории файла.

То есть защита от второго нажатия была объявлена именем и не делала ничего:
форма удаления склада оставалась нажимаемой, пока шёл запрос.

Правило написано ровно по смыслу имени (`pointer-events: none; opacity: .55`)
и ничего к нему не добавляет. Это тот же класс дефектов, что «вторая преграда,
переставшая быть преградой» в P5 §6 и «одиннадцать подписей, не называвших
ничего» в §3.5: объявление есть, поведения нет, и на экране всё выглядит
нормально.

### 5.6. Третья часть: тот же набор ещё дважды

`report` и `dashboard` несли ровно те же шесть компонентов под именами
`fuel002d-*` и `fuel002h-*`. Сопоставление то же, что в §5.4, и применено
дословно.

| Экран | inline-CSS | классы таблиц |
|---|---|---|
| `report` | 143 → **0** | 3 → **0** |
| `dashboard` | 97 → **0** | 1 → **0** |

По проекту: `inline-style` 669 → **429**, `table-class` 13 → **9**,
`hardcoded-color` 130 → **128**.

Итого набор «полоса показателей + карточки-подсказки + шапка + обёртка таблицы
+ таблица + карточка формы» был заведён в модуле АЗС **пять раз**: `002a`,
`002c`, `002d`, `002e`, `002h`. Пять копий одного и того же, каждая со своим
префиксом по коду задачи, каждая чуть-чуть отличается. Ни одна из пяти не была
хуже других — они просто не знали друг о друге.

Осталось в модуле: `warnings` (142 строки, свой набор `warning-*`),
`balance_report` (30, печать) и восемь мелких экранов (37 строк, 12 цветов,
5 классов таблиц).
