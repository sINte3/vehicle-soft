# P5 — Implementation foundation

Начата 2026-08-09, сразу после подписи владельца под P4
(`32-canonical-screens.md` §7).

**Цель фазы:** внести токены и общие компоненты, **не тронув
немигрированные страницы**.

## 1. Что из работ P5 уже было сделано в P4

Список работ фазы в `00-workflow.md` §14 — «токены; иконочный макрос;
`vs-table` с модификаторами; единый `_module_nav.html`; сокращение
брейкпоинтов». Первые четыре пункта закрылись раньше срока, потому что
эталонные экраны без них не собирались:

| Работа | Где закрыта |
|---|---|
| токены, двухуровневая архитектура | UI-FOUNDATION-001 |
| иконочный макрос `_icons.html` | UI-FOUNDATION-002 |
| `vs-table` с модификаторами (DD-028…DD-031) | UI-P4-002 |
| единый `_module_nav.html` | UI-P4-008 |
| **сокращение брейкпоинтов** | **P5, идёт** |

## 2. Брейкпоинты: DD-024

Было 13 значений, из них опасные близнецы 768/760, 720/700, 1100/1050
(PR-004). Шкала — четыре: **1280 / 1024 / 768 / 480**.

Правило переноса из DD-024 соблюдено дословно: **каждое правило перенесено
по своему содержимому, а не по близости числа.**

### `static/css/design-system.css` — 5 → 0

| Было | Стало | Почему именно туда |
|---|---|---|
| 1100 | **1024** | вторая ступень плотности сеток показателей; первая уже стояла на 1280 |
| 900 | **1024** | шкала называет 1024 именно этим переходом — «планшет: сайдбар сворачивается в иконочный рельс». Это оно и есть |
| 1200 | **1280** | первая ступень плиток центра отчётов |
| 720 | **768** | вторая ступень тех же плиток |
| 640 | **480** | собственный комментарий правила говорит «on phones», а не «on tablets». Перенос на 768 расширил бы правило на планшеты, то есть поменял бы его смысл ради красоты числа |

Значения совпадают с матрицей вьюпортов обхода намеренно: съёмка попадает
**внутрь** ветки CSS, а не на её границу. При окне ровно 1024 px правило
действует.

### Шаблоны — 26 → 0

Сделано всё: `ref_*` (8), `wialon_mapping_list` (2), модуль АЗС (16).

Все десять одной формы: верхняя ступень «сетка → 2 колонки», нижняя
«→ 1 колонка». По шкале это 1024 и 480 — второе прямо названо в DD-024
«одна колонка».

**Измерение пришлось чинить до правки, а не после.** Стенд снимал 1440 /
1024 / 390. Полоса, в которой эти правила и меняются — 480…768, — не
снималась вовсе: два вьюпорта выше неё, один ниже. Проверка, не попадающая
в изменённую ветку, даёт одинаковый ответ при верном и неверном коде. В стенд
добавлен вьюпорт **600 px**.

Результат на 600 px: изменились **ровно пять страниц, которые правились**,
и все стали короче — две колонки вместо одной:

| Страница | Доля пикселей | Высота |
|---|---|---|
| `/wialon/mapping` | 22,29 % | 3521 → 3305 |
| `/ref/organizations` | 17,15 % | 2903 → 2724 |
| `/ref/customers` | 17,06 % | 3350 → 3171 |
| `/ref/work_types` | 16,42 % | 3065 → 2886 |
| `/ref/equipment` | 13,10 % | 3657 → 3478 |

Остальные 67 страниц на 600 px — максимум 0,07 %. Вне полосы: 0,03 % на
1440 и 0,08 % на 390.

### Модуль АЗС — 16 → 0

Та же двухступенчатая форма, но нижняя ступень вдобавок разворачивает шапку
вертикально (`flex-direction: column`) и прижимает кнопки влево. Перенос
нижней ступени с 700/760 на 480 означает, что шапка остаётся горизонтальной
до 480 — а если она туда не помещается, это **переполнение по ширине**, а не
«стало иначе».

Пиксельный дифф такого не различает: и то, и другое — просто другие пиксели.
Поэтому перед правкой в стенд добавлено обнаружение переполнения
(`scrollWidth > clientWidth`), и только после этого правила были перенесены.

**Новых переполнений — 0.** Было 23 страницы, стало 23, те же самые.

Результат на 600 px — изменились ровно правленые страницы, все стали короче:

| Страница | Доля пикселей | Высота |
|---|---|---|
| `/fuel/warehouses` | 36,97 % | 1610 → 1369 |
| `/fuel/receipts` | 35,84 % | 1973 → 1381 |
| `/fuel/report` | 34,77 % | 3328 → 3291 |
| `/fuel/stations` | 27,82 % | 2621 → 2358 |
| `/fuel/warnings` | 26,74 % | 12807 → 12386 |
| `/wialon/mapping` | 22,29 % | 3521 → 3305 |
| `/ref/organizations` | 17,15 % | 2903 → 2724 |
| `/ref/customers` | 17,06 % | 3350 → 3171 |
| `/ref/work_types` | 16,42 % | 3065 → 2886 |
| `/ref/equipment` | 13,10 % | 3657 → 3478 |

Ни одной страницы вне этого списка на 600 px: максимум 0,07 %.

### Итог по DD-024

**37 → 0.** Тринадцать значений свелись к четырём: 1280 / 1024 / 768 / 480.
Опасные близнецы (768/760, 720/700, 1100/1050) больше не существуют.

## 3. Критерий выхода 3 — визуальный дифф на немигрированных страницах

**«Фундамент не имеет права менять то, что ещё не мигрировали.
Превышение = откат.»**

Порог в спецификации назван «согласованным», но нигде не согласован.
Предлагается и применён такой: **вне полосы, объявленной изменённой,
дифф ≤ 1 % площади страницы.**

Инструмент — `tools/ux/shoot_regression.mjs`. Снимает 72 немигрированные
страницы с двух экземпляров и считает долю различающихся пикселей.

**База сравнения — подписанный P4** (`0ad962a`), а не состояние до трека.
Это существенно: критерий спрашивает, что натворил **фундамент P5**, а не
весь трек. Первый прогон сделан против пред-трекового коммита и дал медиану
39–47 %, что верно и бессмысленно: там внутри вся работа P4.

| Вьюпорт | Страниц | Медиана | Максимум |
|---|---|---|---|
| 1440 (ноутбук) | 72 | **0,00 %** | **0,03 %** |
| 390 (телефон) | 72 | **0,00 %** | **0,08 %** |
| 600 (узкий) | 72 | **0,00 %** | 22,29 % — пять правленых страниц, §2 |
| 1024 (планшет) | 72 | 39,58 % | 73,81 % |

**Вне полосы 900–1024 фундамент не изменил ничего** — максимум 0,03 %, и это
текст времени («последняя синхронизация»), а не вёрстка.

**Внутри полосы 900–1024 изменение одно и намеренное**: сайдбар сворачивается
в иконочный рельс на 1024 вместо 900, как требует шкала DD-024. Контентная
область сдвигается влево примерно на 178 px, поэтому доля различающихся
пикселей велика на каждой странице. Это одна причина, а не 72 регрессии;
пиксельная доля для глобального сдвига раскладки — плохая метрика, и её
число здесь не следует читать как «сломалось 40 % экрана».

**Найдено при отладке измерения:** `/admin/audit` дал 21,82 % на ноутбуке —
единственный выброс, выглядевший как регрессия. Сравнение текстом показало
37 совпавших ячеек из 40 при разном ЧИСЛЕ строк: журнал аудита пишет каждый
вход, а краулер логинится в каждый экземпляр своё число раз. Страница
исключена из набора с указанием причины прямо в скрипте.

## 4. Критерий выхода 4 — слой совместимости сократился

**244 → 225 строк.** Удалено **29 правил**, ни одно из которых не
используется ни одним шаблоном, ни одним скриптом, ни одним генератором
разметки на Python.

Это были утилиты в духе Bootstrap — `.d-flex`, `.gap-2`, `.mb-3`,
`.fw-bold`, `.text-success`, `.alert-info`… — заведённые на всякий случай и
не понадобившиеся ни разу.

Проверка перед удалением: полный поиск по репозиторию (html/py/js/md) плюс
отдельный поиск динамической сборки имён классов в JS — совпадений ноль.
Проверка после удаления: пиксельный дифф по 72 страницам не изменился ни на
сотую долю процента. Правило, удаление которого ничего не меняет, и было
мёртвым.

Слой существует, чтобы **немигрированные** страницы продолжали работать.
Правило, к которому никто не обращается, этой задаче не служит и только
делает вид, что слой больше, чем есть.

## 5. Критерии выхода — состояние

| # | Критерий | Состояние |
|---|---|---|
| 1 | `compileall`, `check_templates.py`, его самотест — чисто | **выполнен** |
| 2 | все тесты зелёные, ни один не удалён и не ослаблен | **выполнен** — 518 тестов |
| — | брейкпоинты (список работ, не критерий выхода) | **выполнен** — 37 → 0 |
| 3 | визуальный дифф на немигрированных ≤ порога | **выполнен** — 0,03 % вне объявленной полосы |
| 4 | `COMPATIBILITY LAYER` сократился на измеренное число строк | **выполнен** — 244 → 225, 29 правил |

**Фаза закрыта.** Все четыре критерия выполнены, весь список работ P5 сделан.

Порог по критерию 3 предложен здесь, а не согласован заранее, — это стоит
знать при чтении: **вне полосы, объявленной изменённой, ≤ 1 %**. Фактически
0,03 % на 1440 и 0,12 % на 390. Внутри объявленных полос изменения названы
поимённо выше, и каждое — следствие одного решения DD-024, а не россыпь
регрессий.

## 6. Дефект, найденный в собственной работе P4

При чтении `_module_nav.html` обнаружено, что скобки в единственном пункте с
двумя правами разъехались при подстановке защиты от анонима:

```
было задумано: _auth and (A or B)
получилось:    _auth and A or B    →    (_auth and A) or B
```

`and` связывает сильнее `or`, поэтому B вызывался у анонимного пользователя
**всегда**. Для вошедшего результат тот же, и ошибка не проявлялась ничем —
кроме падения страницы входа, куда шаблон не заходит, пока импорт стоит
внутри ветки для авторизованных. То есть объявленная вторая преграда молча
перестала быть преградой, а комментарий рядом продолжал обещать, что она
работает.

Исправлено. Заведён тест `test_the_second_barrier_actually_holds`: шаблон
рендерится анонимом **напрямую**, вне всякой оболочки. Отрицательный
контроль — возврат сломанных скобок роняет ровно этот тест.

Попутно тест поймал ещё один дефект уже в себе: проверка по подписи падала
на верном коде, потому что «Омбор» (Склад, закрыт правом) входит подстрокой
в «Омбор карточкаси» (Карточка склада, открыта всем). Сравнение переведено
на адреса.
