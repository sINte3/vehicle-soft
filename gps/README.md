# GPS-ENGINE-001 — движок расчёта обработанной площади

Первый кодовый инкремент трека «GPS план-факт». Считает, сколько гектаров
машина фактически обработала за интервал, и выдаёт вместе с цифрой флаг
качества трека.

Пакет **не является частью Flask-приложения**. Он не импортирует ни `app`,
ни `models` и держит свой `requirements.txt` — по тому же правилу, что и
`drone_collector`: служба не должна тащить numpy/scipy из-за модуля, который
она не импортирует.

`area.py` не открывает `transport.db` вовсе. `daily.py` (GPS-2) в него
**пишет** — две таблицы, созданные миграцией `migrate_gps_daily_001.py`, —
но через stdlib `sqlite3`, а не через `models`: `app = create_app()` вызывает
`db.create_all()` на импорте, и любой скрипт с `from app import app`
становится писателем. Приложение эти две таблицы только читает.

---

## Метод

```
точки интервала
  → фильтр: скорость 1–15 км/ч
  → сгущение пути каждые 5 м (только сегменты короче 150 м)
  → альфа-форма: триангуляция Делоне, треугольники с радиусом
    описанной окружности < альфа
  → объединение треугольников
  → отдельные куски объединения = участки работы; куски меньше порога — проезд
  → площадь в UTM 41N (EPSG:32641)
```

### Геозона методу не нужна — изменено 12.08.2026

Операторы на местах геозоны обычно **не заводят**: обводят трек по периметру
обработки вручную и записывают цифру. Метод, считавший только внутри
заведённого полигона, поэтому терял всё, что никто не завёл: на наборе по
опрыскиванию это **31,77 га из 159**, и две работы из пятнадцати выходили
ровным нулём, пока трактор весь день был в поле.

| вариант | медиана \|расхождения\| |
|---|---|
| с обрезкой по контуру, альфа 10 м | 26,4% |
| с обрезкой по контуру, адаптивная альфа | 12,0% |
| **без контура, участки по треку** | **6,9%** |

Работа 08.08 была −100%, стала +2,4%; 05.08 была −92,3%, стала +10,1%.

`work_sites()` — основной вход. Геозона, если она есть, только **называет**
участок для счёта: не фильтрует и не обрезает. `worked_area()` остаётся для
случая, когда вопрос задан про конкретный контур.

Параметры — константы, а не настройки. Метод зафиксирован письменно
2026-07-29 и проверен на наборе, которого он не видел: медиана расхождения с
ручным замером владельца **3,3%** на 17 работах. Каждое число измерено, а не
выбрано: точки вдоль прохода стоят через 35–55 м, соседние проходы — через
5–14 м, и именно этот разрыв масштабов делал параметр хрупким. Сгущение
убирает разрыв, после чего альфа устойчива в диапазоне 8–15 м.

### Адаптивная альфа — введена 12.08.2026

Правило `альфа = max(10; 1,2 × измеренный шаг проходов)` было записано
29.07 и тогда **не принято**: оно подобрано на тех же 17 работах, которые
улучшало. Предрегистрация говорила — правило входит в метод, только если
подтвердится на невиданном наборе. Набор пришёл 12.08: 15 ручных замеров по
опрыскиванию, расчёт замороженным кодом, закоммиченным **до** данных.

| | медиана \|расхождения\| | среднее |
|---|---|---|
| фиксированная альфа 10 м | 26,4% | −32,1% |
| адаптивная | **12,0%** | −22,9% |
| адаптивная, 8 работ без неразмеченной земли и рваного трека | **3,4%** | — |

Устойчивость проверена по обоим параметрам оценщика: признак «другой
проход» (отношение 3 / 5 / 8) не меняет результат вообще, множитель
1,1 / 1,2 / 1,3 двигает медиану в пределах 10,1–12,0%.

И правило **ничего не портит там, где метод уже подтверждён**. У
опрыскивателя шаг 13–22 м, у культиватора 5–9 м, поэтому `max(10; 1,2×8)`
даёт те же 10 — на культивации правило математически холостое. Проверено
переигровкой: калибровочный набор 18–23.07 сдвигается **ровно на +0,0%**
(ни одна работа не изменилась), набор 27.07 — на +0,9%, и все сдвинувшиеся
работы там опрыскивание.

Чтобы воспроизвести старое поведение точно, передайте `alpha_m=ALPHA_M`.

**Чего набор не закрыл:** 7 работ из 15 остаются неверными по причинам, к
альфе не относящимся — до 8,8 га обработано по земле, на которой вообще нет
геозоны, и треки теряют до 708 минут движения в сутки. Это дефекты входных
данных; движок их измеряет и показывает, но исправить не может.

**Контур определяется только по треку** — какие полигоны содержат точки
машины. По имени нельзя: справочник Wialon содержит дубли по конструкции,
«1508 Нурхон Бобохон» существует двумя зонами (4,32 и 8,97 га), и поиск по
точному имени однажды выбрал ту, где трактора не было ни одной точкой.

## Чего движок не делает

В нём **нет бизнес-правил**. Он не решает, что считать работой, а что
проездом; какое расхождение допустимо; когда трек слишком рваный, чтобы
выставлять счёт. Это решения владельца (вопросы В-2 и В-3 в
`docs/GPS_PLAN_FAKT_VISION_ROADMAP.md`). Движок возвращает измеренные числа,
пороги накладывает вызывающий код.

## Установка и запуск тестов

Отдельное окружение, не окружение приложения (PowerShell, по одной строке):

```
& "C:\Program Files\Python314\python.exe" -m venv C:\gps_venv
```

```
& C:\gps_venv\Scripts\python.exe -m pip install -r C:\transport-report\gps\requirements.txt
```

```
cd C:\transport-report
```

```
& C:\gps_venv\Scripts\python.exe -m unittest discover -s gps/tests -t .
```

Ожидается `Ran 72 tests` и `OK`.

## Как проверен

**Синтетика** — ответ известен по построению: квадрат 100×100 м, пройденный
проходами через 5 м, даёт 0,9875 га (недобор 1,25% — граничный эффект
альфа-формы, свойство метода); необработанная полоса 30 м не заклеивается;
разрыв 240 м не сшивается; проезд на 30 км/ч даёт 0 га; вырез в контуре
шириной 3 м не попадает в площадь.

**Реальная фикстура** — зона 3208 и трек юнита 3464 за 27.07.2026:
воспроизводится записанная цифра **8,51 га** при 939 рабочих точках, и
соседняя зона-однофамилец даёт ноль (отрицательный контроль опознания
контура по треку).

**Метрика качества трека переписана трижды, и каждый раз ошибку находило
измерение, а не рассуждение.** Считать разрывы по всем сообщениям — стоянка
становится «рваным треком» (26 ложных разрывов у юнита 3068). Считать по
подряд идущим движущимся точкам — стоянка возвращается, потому что сообщения
между ними не смотрят (23 ложных разрыва и 5,9 «потерянных» часа у юнита 3464
при полностью целом треке). Требовать движения на любом из концов разрыва —
считается ночная стоянка: трактор встал, двигатель заглушен, трекер уснул, а
утреннее сообщение показывает движение (708 «потерянных» минут у юнита 1729
за 05.08, где все 13 разрывов начинались на нуле).

Верно: разрыв — это молчание трекера **на ходу**, то есть скорость в
последней точке перед провалом больше нуля. По всем 37 машино-дням остаётся
5 настоящих разрывов на 126 минут против 56 стоянок на 3777 минут, которые
считало прежнее правило.

Проверено на трёх реальных треках: у 3068 наивный счёт 26 → верный 0, у 7242
5 → 0, у 1729 за 08.08 наивных 14, а настоящий ровно один — 52 минуты
молчания на ходу. Метрика, всегда возвращающая ноль, прошла бы первые две
строки и провалила третью.

**Мутационная проверка** (2026-08-12) — набор обязан ловить порчу движка.
Одиннадцать мутаций, каждая поймана:

| Мутация | Упало тестов |
|---|---|
| сгущение выключено | 8 |
| альфа раздута до 100 м | 6 |
| верхняя граница скорости 100 км/ч | 1 |
| метрика разрывов возвращена к наивной | 2 |
| обрезка по контуру убрана | 1 |
| нижняя граница скорости 0 (холостой ход) | 4 |
| фильтр «внутри контура» убран | 1 |
| множитель альфы 1,2 → 1,0 | 1 |
| шаг проходов игнорируется (всегда альфа 10) | 4 |
| оценщик берёт максимум вместо медианы | 5 |
| признак «другой проход» выключен | 1 |

**Дважды мутации ловили один и тот же дефект набора: ожидание, записанное
через саму константу, уезжает вместе с мутацией.** Так тест проезда
(`SPEED_MAX_KMH + 15`) и тест альфы (`ALPHA_SPACING_FACTOR * 14`) молча
проходили на испорченном коде. Оба переписаны на литералы — 30 км/ч и 16,8 м.
Ещё одна мутация (обрезка по контуру) не ловилась ничем — добавлен тест
контура с вырезом.

## Аномалии трекера — «звёзды»

Трекер, ненадолго потерявший небо, отдаёт точку, где машина не была.
Признак: шаг между точками требует скорости выше **заявленной самим
трекером** более чем на 40 км/ч. Сравнение идёт с его же показаниями, а не с
фиксированным пределом, поэтому грузовик на 90 км/ч не помечается.

Переход считается прыжком, только если между сообщениями **не меньше 5
секунд**. Без этого условия признак ловил сбитые отметки времени: на
МТЗ 572 HA 295 переходов выглядели невозможными, а настоящими были два —
остальные оказались парами сообщений в одну секунду, где 30 м это обычный
шаг, втиснутый в плохой таймстамп.

Счётчик `gps_jumps` и доля `jump_share` лежат в флаге качества; порог, за
которым день объявляется недостоверным, движок не решает — это
бизнес-правило. Настоящие прыжки редки: по 32 машино-дням их два.

Здоровье трекеров в целом меряет `tools/gps_tracker_health.py` — там четыре
показателя, и решающими оказались не прыжки, а число спутников и частота
записи.

## GPS-2 — суточный расчёт (`gps/daily.py`)

Берёт точки одного объекта за одни местные сутки из помесячного файла, который
пишет коллектор GPS-1 (`gps_collector/`), и кладёт результат в две таблицы
`transport.db`:

- `gps_daily_aggregates` — **всегда по строке на объект-сутки**, в том числе
  когда работы нет. «Мы посмотрели, и работы нет» и «мы не смотрели» — разные
  факты, и отсутствующая строка их не различает. Причина отказа лежит в
  колонке `reason`: `redkaya_zapis`, `net_dvizheniya`, `net_tochek`; `NULL` —
  площадь опубликована.
- `gps_work_polygons` — по строке на найденный участок: площадь, минуты на
  участке, полигон в GeoJSON (WGS84), контур (может быть `NULL` — работа по
  земле без геозоны это норма), альфа, шаг проходов, флаг качества,
  `suggested_label` и `operator_label`.

**Отказ считать — обязательная часть.** Если медианный интервал записи за
сутки больше **30 с**, площадь не публикуется: прореживание собственных
треков показало, что редкая запись всегда занижает площадь и никогда не
завышает — 5,5% в медиане при удвоении интервала и до 45% на мелких работах.
Заниженный гектар в счёте хуже, чем гектар не выданный.

Сравнение **строго больше**, и это несущая деталь: 24% полевой техники пишет
ровно раз в 30 с, и проверочная фикстура 3464 за 27.07 — тоже. Замена на `>=`
выбрасывает настоящий проверенный день; тест это ловит.

**Ответ оператора переживает пересчёт.** Пересчёт суток заменяет строки, а не
добавляет вторые, и переносит `operator_label` на новый участок **по площади
пересечения**, а не по номеру участка: номера съезжают при любом изменении
входа, а ответ, приклеенный к чужому куску земли, хуже потерянного — про него
никто не узнает. Ответ, которому не нашлось нового участка, считается и
печатается, а не исчезает молча.

`suggested_label` — предрегистрированное правило «площадь ≥ 1,3 га ИЛИ время
на участке ≥ 25 минут» (§2.8.1 дорожной карты). Это **подсказка**, а не
вердикт: пороги заморожены, судит их `tools/gps_label_evaluate.py`, и тест
сверяет константы с ним, чтобы правило нельзя было подвинуть в одном месте.

Запуск (нужно окружение с геостеком):

```
& C:\gps_venv\Scripts\python.exe -m gps.daily --date 2026-07-27
```

### Как проверен GPS-2

| Критерий приёмки | Чем проверен |
|---|---|
| Фикстура 3208/3464 за 27.07 | `FixtureDay` — 8,772 га, 317,4 мин, 892 точки внутри; те же числа независимо даёт `tools/gps_label_sites.py` |
| Пересчёт заменяет строки и не затирает ответ | `Recomputation` — десять пересчётов дают одну строку; ответ переносится; отрицательный контроль: ответ без своей земли считается потерянным |
| Машина с интервалом 301 с | `RefusalPaths` — причина `redkaya_zapis`, ноль полигонов |
| Сутки без движения | `RefusalPaths` — агрегат есть, причина `net_dvizheniya`, ноль полигонов |

**Про 8,51 га.** Эта цифра измерена `worked_area()`: точки фильтровались по
геозоне и результат обрезался по ней. Метод, принятый 12.08 (§2.4 и §2.6
дорожной карты), не делает ни того, ни другого — обрезка теряла 31,77 га из
159 и давала два ровных нуля при машине, стоявшей в поле весь день. Поэтому
GPS-2 на той же фикстуре публикует **8,772 га**, а не 8,51. Обе цифры
закреплены тестами рядом: 8,51 по-прежнему воспроизводится обрезанным
вызовом, и разница 0,27 га — это смена метода, а не регресс.

Мутационная проверка GPS-2 (2026-08-19), каждая мутация поймана:

| Мутация | Упало тестов |
|---|---|
| порог редкой записи стал `>=` | 14 |
| порог правила 1,3 → 2,0 га | 2 |
| ответы оператора не переносятся | 1 |
| старые полигоны не удаляются при пересчёте | 3 |
| снят потолок 300 с при счёте минут | 3 |
| проверка «нет движения» убрана | 1 |
| полигон записан в метрах UTM вместо градусов | 2 |
| правка без изменения поведения (контроль) | 0 |

---

## Сквозная проверка цепочки

`gps/tests/test_end_to_end.py` прогоняет **настоящий** трек 3464 за 27.07 через
весь тракт: консервированный Wialon → коллектор → `messages/unload` → watermark
→ помесячный файл → суточный расчёт → миграция → строки в `transport.db`.

Записанные **8,772 га** обязаны выйти на том конце. Остальные наборы проверяют
куски по отдельности и швов между ними не видят вовсе — а ломаются обычно
именно швы: сообщение теряет поле по дороге в хранилище, помесячный файл
читается не за те сутки, координаты меняются местами. На сервере такой дефект
обнаружился бы утром после первого прогона, и разбирать его пришлось бы по
пустому экрану.

Проверка умеет падать, и это показано порчей кода (2026-08-19):

| Мутация на шве | Упало тестов |
|---|---|
| долгота и широта переставлены на приёме | 3 |
| сутки читаются наполовину | 4 |
| точки кладутся не в свой месяц | 6 |
| спутники теряются по дороге | 1 |
| `messages/unload` не вызывается | 1 |
| правка без изменения поведения (контроль) | 0 |

---

## Дальше по треку

Движок — расчётное ядро. На нём стоят: суточный расчёт `gps/daily.py` (GPS-2,
сделан), экран «Факт по технике» (GPS-3) и сверка нарядов. Порядок и критерии
готовности — в `docs/GPS_PLAN_FAKT_VISION_ROADMAP.md`.
